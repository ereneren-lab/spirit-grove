node_modules/
.DS_Store

# 빌드 산출물 — Actions가 서버에서 빌드하므로 커밋 불필요
/index.html

# ── 저장소 정리(2026-07-28) ────────────────────────────────────────────
# ⚠️ 아래는 **로컬에는 그대로 두고 추적만 뺀 것들**이다(git rm --cached).
#    지운 게 아니라, 다른 기기에서 클론할 때 헷갈리거나 무거워지지 않게 뺐다.
포켓몬/            # 아트 원본 114MB·91개 — 파이프라인 입력은 art_inbox/를 쓴다
undefined/         # 경로가 잘못 생성된 흔적(날씨 아이콘 png 4개) — 코드에서 참조 없음
WORKLOG 2.md       # WORKLOG.md의 오래된 중복본
