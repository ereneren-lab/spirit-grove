node_modules/
.DS_Store

# 빌드 산출물 — Actions가 서버에서 빌드하므로 커밋 불필요
/index.html

# ── 저장소 정리(2026-07-28) ────────────────────────────────────────────
# ⚠️ 아래는 **로컬에는 그대로 두고 추적만 뺀 것들**이다(git rm --cached).
#    지운 게 아니라, 다른 기기에서 클론할 때 헷갈리거나 무거워지지 않게 뺐다.
# ⚠️ .gitignore는 **줄 끝 주석을 지원하지 않는다** — 패턴 뒤에 `# ...`를 붙이면
#    주석까지 패턴의 일부가 되어 아무것도 안 걸린다(실제로 겪음). 설명은 반드시 윗줄에.

# 아트 원본 114MB·91개 — 파이프라인 입력은 art_inbox/를 쓴다
포켓몬/
# 경로가 잘못 생성된 흔적(날씨 아이콘 png 4개) — 코드에서 참조 없음
undefined/
# WORKLOG.md의 오래된 중복본
WORKLOG 2.md
# 대조표를 쪼갠 중간 산출물 — split_contact_sheet.py가 원본에서 언제든 다시 만든다
art_inbox/npc/_split/
# 채택 안 된 후보 — 바닥(잔디)이 그려져 있어 지역마다 따라다닌다
art_inbox/terrain/_rejected/
# 대조표를 쪼갠 중간 산출물
art_inbox/terrain/_split/
