# 작업 로그 (WORKLOG)

> **세션 시작 시 CLAUDE.md 다음으로 이걸 읽어라.**
> CLAUDE.md = 영구 지식(아키텍처·규칙·함정, 잘 안 변함).
> WORKLOG.md = 휘발성 상태(지금 어디까지 했나, 다음에 뭘 할까).
>
> **갱신 규칙**: 커밋할 때마다 「현재 상태」를 고치고 「세션 기록」 맨 위에 한 블록 추가.
> 작업이 중간에 끊길 것 같으면 **미완 상태 그대로** 「진행 중인 작업」에 적어둘 것.

---

## 📌 세션 인수인계 → **[`NEXT_SESSION.md`](NEXT_SESSION.md) 를 먼저 읽어라**

지금 뭘 하면 되는지(남은 작업·명령·판정 기준)는 전부 그 파일에 있다. 아래는 그 배경이 되는
2026-08-04 기준 원본 블록이고, **①②는 여전히 유효**하다(③은 NEXT_SESSION.md가 대체한다).

**① 저장소가 옮겨졌다.** 실제 위치는 **`~/Desktop/_보관/게임프로젝트/spirit-grove`** 다.
옛 경로 `~/Desktop/spirit-grove/`는 **`.claude/settings.local.json`만 남은 빈 껍데기**이고 `.git`도 없다.
⚠️ 거기서 git 명령을 쓰면 상위의 **`~/Desktop/.git`(데스크톱 자체가 git 저장소다)** 으로 올라가
엉뚱한 저장소를 건드린다 — 실제로 `git status`가 `sa up/ANALYSIS.md`를 보여준 적이 있다.
⚠️ 에이전트 메모리는 **경로별로 저장돼 이 이동으로 끊긴다.** 그래서 이 블록을 저장소 안에 남긴다.

**② 디자인 작업은 자동 위임으로 한다.** 사용자가 `@design-researcher` 식으로 직접 부르지 않는다.
목표만 받으면 Orchestrator가 결과물 유형(BX·그래픽·UX/UI·영상)을 판별하고
`design-researcher → design-strategist → creative-producer` 순으로 알아서 넘긴다.
브리프에는 CLAUDE.md 「에이전트 전달 원칙」의 11개 항목을 통째로 실어 보낸다
(**서브에이전트는 이전 대화를 모른다고 가정**한다). 병렬 가능/순차 필수 구분도 그 문서를 따른다.
⚠️ 핵심 전략은 사용자 승인 없이 확정하지 않되, **사소한 건 가정을 표시하고 진행**한다(작업을 멈추지 않는다).

**③ 미완**(2026-08-04 시점): fullrun 완주 재검증. → **08-05에 진행했고 상태가 바뀌었다** —
원인이 봇이 아니라 **게임**이었고(`findTile` 캐시가 제단 두 곳을 사슬에서 지웠다), 고쳐서 배포까지 마쳤다.
**현재 남은 것과 다음 수순은 [`NEXT_SESSION.md`](NEXT_SESSION.md) §2 를 볼 것.**
⚠️ 이 맥은 상시 부하가 높다(load 4~8/8코어). **테스트 전에 `pkill -f headless_shell`로 고아 프로세스를
걷어낼 것** — 실제로 부모가 죽은 headless_shell 6개가 로드를 8까지 올리고 있었다.

---

## 현재 상태 (2026-08-10 기준)

- ✅ **출신지 4곳이 완성됐다** — 지도·도입·나오는 자리·부활 지점·**인물 대사**까지.
- ⛔ **fullrun 완주 검증은 접었다** — 이 맥에선 브라우저가 죽어 결론이 안 난다. 결정적 테스트 6개로 대신한다.
- 🎨 **신규 정령 4종은 아트 대기** — 데이터·진화 연결·프롬프트 확정. 그림 4장만 오면 들어간다.
- ✅ **초반 완급·공간 활용을 고쳤다** — 체육관을 가로로 흩고(폭 0→20칸), 첫 체육관을 집 앞에서 떼어냈다(토리 3보→19보).
  그 길에 트레이너 3명을 다시 깔았다(넷 다 3명씩 만난다, 첫 전투 5보 이후).
- ✅ **NPC 실루엣이 갈렸다** — 체형·머리길이·모자·소품 네 축. 조합 12종 → **34종**, 같은 실루엣 최대 무리 33명 → 3명.
  📌 **색은 다양성이 아니다** — 옷 색만 다르면 멀리서 다 같은 사람으로 읽힌다.
- 🎨 **NPC 원형 시트 경로가 들어가 있다** — 43명 → 원형 12종. **그림 12장만 오면 코드 수정 없이** 붙는다.
  프롬프트·절차: `outputs/production/2026-08-11_spirit-grove_npc-sheet-prompts.md`
  ⚠️ 3자세 한 장으로 받는다(9칸 시트 아님). 변환기가 걷기 프레임을 합성한다.
- 🧭 **좌표의 단일 출처는 `GYM_AT`이다.** 이번에 좌표를 또 들고 있던 곳이 **5군데** 나왔다(코드 1 + 테스트 4).
  ⚠️ **테스트가 든 상수도 병렬 테이블이다** — 코드만 고치면 절반만 고친 것이다.
- **전체 `verify.sh` 통과 · 배포 완료 · 미배포분 없다.**

## 이전 상태 (2026-08-07 기준)

- ✅ **주인공 4명이 픽셀이다** — 3×3 시트 걷기 애니까지.
  ⚠️ **정령 86종의 픽셀화는 되돌렸다(2026-08-10).** 유저: "그냥 모자이크 처리된 것 같고 별로야."
  나란히 놓고 확인해보니 실제로 눈이 회색 덩어리로 뭉갰다. 중간값 필터+아웃라인+채도 보정으로
  다시 시도했지만 여전히 안 됐다 → **86종 전부 페인터리 원본으로 복원.** 빌드 1.2MB → **3.8MB**.
  📌 진짜 픽셀로 가려면 **변환이 아니라 손으로 찍은 도트**가 필요하다(초반 12종부터가 현실적).
- ✅ **출입구가 보인다** — 건물 문 강조 + 정면 칸 발판, 자연 입구·루트 입구엔 빛+고리.
  회귀가 "맵에 있는 입구가 전부 표시 대상인가"를 자동으로 잰다.
- ✅ **캐릭터별 다른 시작** — 출신지 4곳(연구소/야영지/농가/빈터), 도입·나오는 자리·부활 지점이 갈린다.
- ✅ **`walkable` 분리** — `terrainWalkable`(지도만)/`walkable`(지도+NPC). 구조를 묻는 단정은 앞엣것.
- **전체 `verify.sh` 통과 · 배포 완료 · 미배포분 없음.**
- ⚠️ **출신지 인물 대사가 아직 비어 있다**(다음 작업). ⚠️ **맵을 여러 번 고쳤으니 fullrun 한 판은 봐야 한다.**

## 이전 상태 (2026-08-06 기준)

- ✅ **물리/특수가 기술 단위가 됐다** — `MOVES.cat` 덮어쓰기 + `moveCat()` 단일 출처.
  10타입 중 **7타입이 물리·특수 공격기를 모두 갖는다**(전엔 모든 타입이 한쪽뿐이었다).
  UI 3곳에 계열 표기. 회귀 `movecat_test`.
  ⚠️ **종반이 소폭 어려워졌다**(챔피언 -1~4.6%p · 리그 Lv54 완주율 -4~6%p) — 챔피언 선두 집게왕이
  세졌기 때문이고 **의도한 결과**다. 단정은 전부 통과. 되돌리려면 집게왕 기술셋을 원복.
- ✅ **묶음 C 배포 완료** — 라이브에 지닌물건 24종이 다 올라가 있다.
- ✅ **야생/트레이너 지닌물건 풀 분리** — `WILD_HELD`(확장) / `TRAINER_HELD`(동결).
  이제 **야생 풀을 넓혀도 트레이너는 불변**이다. 회귀 `held_effect_test [5]`가 호출부 3곳을 실경로로 단정한다.
  ⚠️ 트레이너를 세게 하려면 `TRAINER_HELD`를 고쳐야 하고, **그건 밸런스 재측정 대상**이다.
- ✅ **fullrun 완주 성공 — 숲의 군주를 거쳐 챔피언까지.** `36.0분 · 전투 164회 · 런타임 에러 0`
  (🏁 gym1 269초 · gym2 882 · gym3 1076 · gym4 1663 · **숲의 군주 1950** · 👑 챔피언 2157).
  **WORKLOG가 오래 들고 있던 미완 항목이 닫혔다.** 진행 곡선의 28~35분 (9,23)은 호숫가 —
  **전설 준비 단계까지 실제로 밟고** 갔다. 08-05에 고친 세 가지(캐시·물길·봇 재시도)가 한 판에서 전부 작동했다.
  ⚠️ 이 기록은 **군주전을 포함한 첫 완주**다(예전 49.2분 기록엔 군주가 빠져 있었다).
- ✅ **묶음 A·B·C 전부 완료** — 성격 배선 누락 · 트래커 도달성 감사 일반화 · 지닌물건 19→24종(아래).
  ⚠️ 세 묶음 모두에서 **"표에 적혀 있는데 코드가 안 읽는" 필드**가 나왔다(성격의 spa · 지닌물건의 recoil).
- **전체 `verify.sh` 실패 0**(4구간).

## 이전 상태 (2026-08-05 기준)

- 🐛 **게임 버그 2건을 잡았다**(둘 다 목표 사슬을 조용히 끊고 있었다) — `findTile` 영구 null 캐시 ·
  물로 둘러싸인 목표의 자동 이동 불가. 아래 세션 기록 참조. 회귀 `lord_route_test` **[7][8]** 신설.
- ✅ **전체 `verify.sh` 실패 0**(4구간: 1-22 · 23-45 · 46-58 · 59+). 테스트 노이즈 2건도 같이 제거
  (`twoturn_test` 명중 5% 플래키 · `door_front_test`가 로밍 NPC를 지도 결함으로 오독).
- ⚠️ **아직 배포 안 함** — 라이브(`b76d505`)에는 위 두 버그가 그대로 있다. **배포하면 바로 이득**이다.
- ⚠️ **fullrun 완주는 여전히 미검증**이다. 다만 **막힌 지점이 게임에서 봇으로 옮겨갔다**:
  4차 판까지 게임 쪽 경로(호수 제단 → 물길 → 군주의 제단 문 앞)는 봇이 실제로 걸어서 도달했다.
  남은 건 봇이 제단 문을 못 여는 것(게임 무혐의 — 같은 좌표·상태로 ArrowUp이면 입장, 프로브·[3] 검증).
  ⚠️ 이 맥에서 90분 판은 **브라우저가 환경 압박으로 죽는 일이 잦다**(4차 판이 43.8분에 사망).

## 이전 상태 (2026-08-04 기준)

- **서사 보강 3단계 + 전체 감사 17건 완료** — 자세한 건 아래 「2026-08-04」 세션 기록.
  **라이브 최신 반영됨**(배포 스냅샷 `b76d505`, Actions 성공) — https://ereneren-lab.github.io/spirit-grove/
- **전체 `verify.sh` 실패 0**(4구간 분할, 75+ 테스트). 회귀 4종 신설(gate·opening·text_quality·lord_route).
- ⚠️ **fullrun 완주 재검증은 미완**이다 — 봇이 제단을 건너뛰던 결함은 고쳤으나(`400335f`) 이후 판이
  4뱃지에 못 가 그 수정이 실행되지 않았다. 부하 낮을 때 한 판 더 돌릴 것.
- 아래는 그 이전(2026-07-29) 상태다.

## 이전 상태 (2026-07-29 최종 기준)

- **브랜치**: `main` / 배포 `scripts/deploy.sh`. 워킹 트리 clean.
  **라이브 최신 반영됨** — 원격 배포 스냅샷 `025156f`(07-29 16:45)가 그날 마지막 콘텐츠 커밋
  (`ff8daf3` 16:44) **직후**라 아래 평가 후속 7건까지 전부 라이브에 있다.
  dist 3.93MB — https://ereneren-lab.github.io/spirit-grove/
- **게임 디자인 평가 후속 7건 완료**(`aa43c50`~`ebc4890`) — 데이터 실측으로 갭을 찾아 순서대로 처리.
  전설 준비 단계 · 땅/독 로스터 복구 · 타입별 공격기 ≥6 · 학습셋 4→8 · tier3 압축 · 초반 밀도 ·
  진화 라인 8개. **각 항목의 '왜'는 커밋 메시지에**, 요약은 `docs/SPIRIT_GROVE_GUIDE.md`
  「게임 디자인 평가 후속 7건」. 아래 세션 기록 (3) 참조.
- **지선 루트 7개**로 오버월드가 입체화됨: 버려진 갱도(숲↔깊은 숲) · 바람 언덕길(초원↔숲) ·
  호숫가 잔교(수정 호수 내, 물 위 판자+낚시) · 서리 고갯길(고원↔리그앞, 얼음 미끄러짐) ·
  이끼 골짜기(깊은 숲↔수정 호수, 모닥불 야영지) · 물안개 벼랑길(수정 호수↔고원) ·
  반딧불 오솔길(마을↔초원, **플레이어가 처음 만나는 지선**).
  일곱 루트 모두 두 입구가 원래 도달 가능한 곳 → **게이팅 불변**(회귀가 강제).
- ⚠️ **평가 후속 7건 이후 fullrun 완주를 다시 안 돌렸다** — 2·4·5·7이 트레이너·야생의 강함을
  바꿨고 각 단계마다 `balance_test`·`league_test`는 재측정했지만, **타이틀→챔피언 실입력 통과는
  `a2a7710`(압축·학습셋 확장 이전) 시점 기록이다.** 다음 긴 세션에서 한 판 돌려볼 것.
- **👑 fullrun 챔피언 완주 성공**(`a2a7710`) — 봇이 타이틀에서 챔피언까지 **실입력만으로 49.2분**,
  게임 **런타임 에러 0**. WORKLOG가 오래 들고 있던 미완 항목이 닫혔다. 아래 참조.
- **저장소 정리됨**(`83f2ba5`): 아트 원본·오생성 폴더·중복 문서 추적 해제 + README에 "다른 기기에서 이어받기".
- **가로 패널 회귀 추가**(`c8b4c0c`·`92a2c8c`) — 지도가 가로에서 안 닫히던 유저 제보. 아래 세션 기록 참조.

### fullrun 봇 결함 3건 — 게임 UI가 바뀌자 봇이 조용히 멈춰 있었다 (2026-07-29)

WORKLOG가 남겨둔 **"60분 판이 뱃지 0 · 원인 미특정"** 을 `TRACE=1`로 1분 만에 특정했다.
지난 세션의 UI 변경에 봇이 옛 배선을 쓰고 있었고, **게임은 정상**이다(누적 게임 버그 집계 불변).
- **①포획이 볼 선택 가방을 연다** → 봇은 `bagOverlay`면 무조건 ✕로 닫았다 → 포획→닫기→포획 **무한 핑퐁**.
  같은 이유로 **회복약도 죽어 있었다**(대상 선택 단계를 몰라 항상 실패로 세고 5회 뒤 영영 포기).
- **②벽시계의 70%가 메시지 대기**(판당 21초). 빨리감기(SKIP)는 `#battleField` **pointerdown 홀드**라
  Enter로는 안 걸린다 → busy 동안 홀드. 판당 3.6초로, 같은 5분에 **전투 10 → 31회**.
- **③상점에서 볼만 사고 회복약은 안 샀다** → 5분에 파산·도주 10회.
- ⚠️ **판마다 편차가 크다** — 같은 코드로 12전투/뱃지0(전멸 4)과 38전투/뱃지2가 나온다. **한 판으로 판단 금지.**
- ⚠️ **교훈**: 게임 UI를 바꾸면 봇 하네스가 조용히 죽는다. 런타임 에러도, 실패 단정도 없다 —
  **"진행이 안 된다"는 사실만 남는다.** UI 커밋 뒤엔 fullrun을 짧게라도 한 번 돌려볼 것.

### 잡은 게임 버그 2건 (2026-07-28 · 둘 다 런타임 에러 0으로 조용히 망가져 있었다)
1. **전멸 후 필드에 닫을 수 없는 강제 교체 모달**(`09048d7`) — ✕ 숨김 + Esc/x/B 전부 무효 → 리로드 말곤 탈출 불가.
   `faintMine`의 스테일 체인이 회복된 파티를 보고 `alive>0`으로 오판. → await 뒤 `!G.inBattle` 즉시 return +
   `openSwitch`가 전투 밖이면 안 열림. **fullrun 봇의 45분 판이 여기서 죽어 발견됐다.**
2. **`wildFloor()`가 실내 좌표를 지역으로 읽었다**(`f568f0b`) — 집 안(y=5)→region 6→야생 하한 34.
   실플레이는 `!G.indoor` 가드로 막혀 도달 불가였지만 계산 자체를 고쳤다(스크린샷에 Lv5 앞 Lv33이 찍혀 발견).

### ✅ 완료: fullrun 챔피언 완주 (2026-07-29)
"60분 판이 뱃지 0 · 봇이 또 다른 방식으로 시간을 흘린다"의 정체는 **봇 결함 4건**(16~19)이었다.
고친 뒤 봇이 **처음으로 끝까지 갔다**:

```
🏁  272초  체육관1 뱃지    (파티 2 · Lv14 · 누적 전투  17)
🏁  635초  체육관2 뱃지    (파티 2 · Lv20 · 누적 전투  48)
🏁 1381초  체육관3 뱃지    (파티 3 · Lv29 · 누적 전투 141)
🏁 1868초  체육관4 뱃지    (파티 6 · Lv29 · 누적 전투 182) — 리그 개방
🏁 2952초  👑 챔피언 등극  (파티 6 · Lv48 · 누적 전투 244)
```
전투 244회(야생 190·트레이너 54) · 포획 5 · 회복약 74 · 센터회복 50 · 상점보충 3 ·
그라인딩 257 · 도주 14 · 전멸(추정) 60 · 최종 소지금 21080 · **런타임 에러 0**.

- **이게 증명하는 것**: 이 프로젝트에서 반복해 나온 버그 종류가 "정의는 있는데 플레이에서 닿지 않는다"였는데,
  이제 **타이틀→챔피언 전 구간이 실입력으로 한 번 이어졌다.** 리그(회복 없는 5연전)도, 전멸→부활→재도전
  루프도 60회 견뎠다. 남은 위험은 *측정된 미지*가 아니라 *아직 안 걸어본 선택 콘텐츠*뿐이다.
- ⚠️ **숲의 군주(제단 X)는 목표 트래커 사슬 밖**이라 봇이 안 간다(완주와 무관한 선택 콘텐츠).
- ⚠️ **한 판으로 판단하지 말 것** — 같은 코드로 10분 판이 뱃지 0으로 끝나기도 한다(편차가 크다).
- ⚠️ `verify.sh`에 **여전히 등록하지 않는다** — 완주에 ~50분이고 짧은 판은 뱃지 0으로 끝나 게이트가 못 된다.

### fullrun 봇 결함 19)  건물 문은 정면(아래)에서만 열린다
목표 트래커는 **인접한 아무 보행칸**으로 데려다주는데, 지난 세션에 출입구를 포켓몬식 정면 전용
(`DOOR_TILES="+EHGUSX"`)으로 바꿨다 → 봇이 옆에서 800ms씩 **181회** 부딪히며 60분 판이 체육관2 앞에서 죽었다.
→ 정면 칸으로 먼저 걸어가서 위로 부딪힌다. 자연 입구(D/V/M/J/O/z/^/!/W)는 문이 아니라 예외.

### ⚠️ 반복해서 밟은 함정 (이번 세션)
- **"고쳤다"가 절반인 경우**: 오버월드 NPC만 올리고 실내를 안 봤다 → 유저 지적 2회.
  실내 인물 5곳이 각자 그렸고, **타일 트레이너 29명 중 24명이 이모지**였다.
  → 개별 외형이 아니라 **"공용 렌더러를 지나는가"를 구조 단정**으로 잡아야 재발이 막힌다(`npc_sprite_test` [6][7]).
- **"요소를 만들었다" ≠ "화면에 보인다"**: 트레이너 인트로 스프라이트가 와이프에 덮인 동안 떴다 사라져
  한 프레임도 안 보였다 → 회귀는 **와이프가 걷힌 뒤**를 본다.
- **한 줄 함수 중간의 `//` 주석**이 `const mir=...`를 삼켰다(CLAUDE.md에 이미 경고된 함정) → `/* */`로.
- **테스트 픽스처가 틀려서 실패**한 경우 2건(존재하지 않는 종 id · 모닥불이 아닌 좌표) — 게임이 아니라 픽스처였다.

## 이전 상태 (2026-07-24 기준)

- **브랜치**: `main` / 배포는 `scripts/deploy.sh`(SSH orphan 스냅샷 → GitHub Actions).
- **최신 커밋**: `test(fullrun): 그라인딩 행동 구현`(`1bf6e6b`). 게임 기능 커밋은 급소 사운드(`e0f8f18`)·연전 도장(`52a4e06`).
- **라이브**: https://ereneren-lab.github.io/spirit-grove/ — **급소 사운드 + 연전 도장 반영 배포됨**(집/모바일에서 확인 가능).
- **빌드**: dist 최신. 빌드+문법+오디오+도달성+지역콘텐츠+tower_test GREEN. 전체 `verify.sh`는 분할 실행 권장(PW_FROM/PW_TO).
- **지역**: 특수 지역 21곳.
- **세션 상태(2026-07-24 매듭)**: ① 전투 폴리시 ✅(급소 사운드) · ② 엔드게임 ✅(연전 도장) · ③ 긴 실플레이 = fullrun 봇 3단 개선(팀 구성·볼 보충·그라인딩) ✅ 구현·발동 확인. ⚠️ **챔피언 완주 검증만 남음** — 환경 부하(load 5~15)로 브라우저 조기 종료라 못 함. **저부하에서 `node scripts/fullrun_test.js dist/spirit_grove_3d.html 900`부터 이어가면 됨**(헤더 12·13·14 참조).

### 이번 세션(2026-07-24)에 한 것
1. **급소 사운드**(`e0f8f18`) — `crithit`("챙!" 상승 반짝임)을 히트음 위에 겹침. 중립 상성 급소도 특별하게. `audio_test` 통과.
2. **연전 도장(Battle Tower)** — 챔피언 후 무한 연승 포스트게임. 도장 관장 세라(16,9). 회복 없는 연전, 매 승리 즉시 보상, 전멸=제자리 종료(패널티 없음). 상대 풀은 DEX tier3 파생. `towerBest` 영속. 회귀 `tower_test.js`(7군 통과). 자세한 건 CLAUDE.md.
3. **fullrun 봇: 팀 구성 + 볼 보충 + 그라인딩** — 봇이 단일 정령 죽음 루프였다. 3단 개선:
   - **팀 구성**: 최강 기술 OHKO로 야생을 못 잡던 것(포획 확률 HP 의존: 풀피 6%·30%HP 45%) → 팀<3이면 HP 무관 매 턴 던진다.
   - **볼 보충**(`restockBalls`): 오버월드 S(보행 가능·밟으면 상점)로 걸어가 정령구 구매. 단독 프로브 검증(볼 0→13). → 봇이 **3마리 팀** 꾸리고 gym2 도달(베이스라인 1마리 대비).
   - **그라인딩**(`grindStep`+GYM_LV): 언더레벨이면 gym 안 들어가고 반경8 풀숲에서 레벨업. 발동 6회·전투·에러0 확인.
   - ⚠️ **완주급 장기 검증은 이 환경 부하(load 5~6)로 브라우저가 1~6분에 죽어 못 함**(게임 버그 아님). **저부하 세션에서 그라인딩→gym2 clear→gym3·4→리그 완주 재측정 필요.** 게임 런타임 에러 0(누적 ~2시간 연속 플레이). `fullrun_test.js` 헤더 12·13·14 참조.

### 이번 세션(2026-07-22)에 한 것
1. **실플레이 검증**(playtest 7종) — 확장 후 첫 실측. 게임 코드 버그 0건.
2. **신규 지역 2곳** — 불꽃 분화구(`dc37ec2`, 불 타입 서식지) · 달빛 화원(`f198f1e`, 낮/밤 조우 분기).
   템플릿 4·5번째 재사용. 진입 좌표는 실제 `walkable()` BFS로 "막아도 도달 범위가 자기 자신만 줄어드는 칸"을 골랐다.
3. **기존 버그 1건**(`ab93d35`) — 오버월드 NPC가 **모든 인테리어** 안에 그려지고 있었다(분화구 스크린샷 확인 중 발견).
4. **바다 미니보스**(`b553599`) — 해일군주. NO_WILD 3종 전부 보스로 획득 가능해졌다.
5. **규칙 계층 확장**(`e53c6c8`) — `MOVES`/`DEX`/`makeMon`을 `src/rules/`로. 브라우저 테스트 2건 삭제(같은 커버리지가 0.1초에 돈다).
6. **풀숲 게임필**(`8166cb9`) — 술렁임·하체 가림·달리기 먼지.

### ⚠️ 타일 문자가 고갈됐다 (다음 지역을 만들 때 제일 먼저 볼 것)
a~z·A~Z 중 남은 글자는 `t` **하나뿐**이다. 그래서 이번부터 **미사용 기호 문자**를 쓴다(`^` 분화구 진입 · `*` 분화구 가드 · `!` 화원 진입 · `?` 화원 가드).
- `walkable` 제외목록은 명시적 나열이라 새 문자는 기본 통행 → 진입 타일은 **제외목록에 꼭 추가**.
- ⚠️ **가드 문자는 오버월드에 나타나면 안 된다** — 미니맵이 `isTrainerTile(t)`면 `TRAINERS[t].em`을 찍는다. 진입(오버월드 전용)/가드(인테리어 전용) 문자를 절대 섞지 말 것.
- 기호가 부족해지면 `GYM_AT`처럼 **좌표 스코프 진입 타일**로 가는 게 다음 수순(문자 하나로 무한 확장).

### 지난 세션(2026-07-21)에 한 것 — 큰 줄기 3개
게임을 **"포켓몬과 똑같아지도록"** 확장하는 게 이 세션의 목표였다. 순서대로:
1. **인수인계 4항목 마감** + **전투 페이스 완화**(`BATTLE_PACE=1.35`, 메시지 대기만 늘림).
2. **포켓몬 패리티 대확장 11종** — 조사 기반 고가치 전부. 지닌물건·날씨·교환소·탈출로프·
   콘텐츠팩·도구줍기·포획루프·스탯아이템·배틀기술·낚싯대등급·2턴기·기술제어.
   → **신규 배틀 기술은 전부 범용 TM(플레이어 전용)** 이라 학습셋 불변 → **밸런스 완전 불변**(핵심 원칙).
3. **지역 심화 한 사이클** — 뇌명 봉우리(`a718fc3`, 전기/비행) → 고대 유적(`b50b027`, normal/rock/ground)
   → **미니보스 2종**(`87d04be`, 천공룡·거암왕 = 진화전용 종의 대체 획득 경로).

### 지역 템플릿이 검증됐다 (늪지→봉우리→유적→분화구→화원, 5연속 재사용)
새 지역을 또 만들려면 **CLAUDE.md의 "신규 지역 — 뇌명 봉우리(skyridge)"** 섹션의 19-포인트 배선을 따를 것.
⚠️ 반복해서 밟은 함정 3건(전부 CLAUDE.md에 기록됨):
- 진입 타일은 **게임 실제 `walkable`로 BFS 도달 가능한 칸**에 둘 것(수동 python BFS 근사는 틀린다).
- `reachability_test`는 진입 문자를 **`all` 목록 + `note` 도달 루프 두 곳** 다 추가해야 한다.
- `dex_flavor_test`는 실제 `HABITAT_KO`를 읽는다(병렬 사본 만들지 말 것).

### ⚠️ 남은 패리티 = 일부러 안 한 것 (조사가 skip 권고)
- **필드 상태(terrain)**: 날씨와 평행한 필드상태 시스템 통째 필요, 체감 대비 비용 큼.
- **광역기(spread)**: 듀오 모드만 이득.
- **자전거**: 러닝(G.running)이 이미 커버.
- **체육관 단일타입화**: 밸런스 커브(문서화된 튜닝)를 흔드는 데이터 재조정 — 위험.
- **Substitute(대타출동)**: 로직 절반·UX 절반(가시 인형 없으면 HP바가 얼어 혼란) → 별도 작업으로 보류.
- **트레이드 진화**: P2P 교환이 없고 유일 계보(megalith)가 이미 evolveLv:36으로 대체됨 → 갭 아님(의도적).

### 알려진 것 (다음 세션에서 헷갈리지 말 것)
- `playthrough_test.js`가 `G.busy가 풀려 있다 (true)`로 실패하면 **부하 문제일 수 있다** —
  이미 실제 시계 예산(45초)+`os.loadavg()` 판별로 고쳐놨지만, 코어당 load>2면 경고만 내고 넘어간다.
  HEAD로 되돌려 같은 테스트를 돌려 진짜 회귀와 환경 오탐을 가릴 것.
- 브라우저 테스트가 66개라 부하 크면 한 번에 못 돈다 → `PW_FROM`/`PW_TO`로 구간 분할
  (이번엔 1-22 / 23-45 / 46-end 3구간). 사이에 `pkill -f headless_shell`(⚠️ `chrom` 넓게 잡지 말 것).
- ⚠️ 백그라운드로 `... 2>&1 | tail -N` 하면 tail이 **파이프 종료 시에만** 출력한다 →
  진행 중엔 출력 파일이 비어 보인다. 완료 여부는 `ps aux | grep verify.sh`로 볼 것.

### 다음에 할 만한 것 (우선순위 없음 — 유저 선택 대기)
- **fullrun 완주 재검증** — 평가 후속 7건이 학습셋·종족값·로스터 타입·진화 라인을 다 흔들었는데
  실입력 완주는 그 이전(`a2a7710`) 기록이다. 단위 밸런스는 재측정했지만 **끝까지 이어지는지는 안 봤다.**
  `node scripts/fullrun_test.js dist/spirit_grove_3d.html 3600` — ⚠️ 저부하 세션에서, 바깥 타임아웃은
  예산보다 넉넉히. ⚠️ **한 판으로 판단 금지**(편차가 크다).
- **지선 루트 더 파기** — 7개 다 기믹이 다르고(지하·야외·물 위·얼음·모닥불) 배선이 검증됐다. 오버월드가
  25×50 세로 복도라 아직 일차원적이니 값어치가 크다. 엔진은 임의 크기 맵을 지원해 **위험이 낮은 콘텐츠 작업**이다.
  ⚠️ 다만 **주요 지역 경계는 이번에 거의 다 메웠다** — 다음 지선은 `route_probe.js`로 남은 먼 쌍을 먼저 찾을 것.
- **새 종/진화 라인 확장은 아트가 선행** — 진화 라인을 8개에서 멈춘 이유가 그것이다(위 (3) 참조).
  라인 밖 14종(86 − 72)이 남아 있지만 모티프가 안 이어져 억지로 묶으면 「아트/설계 규칙」 위반이다.
- **지역 더 파기** — 템플릿이 5연속 검증됐다. 다만 **테마가 뭉친 갭은 이번에 거의 소진**했다(남은 미서식 종은
  전설 5종·스타터 물/풀 라인·NO_WILD 3종 정도). 다음 지역을 만들려면 **먼저 타일 문자 전략을 정할 것**(위 「타일 문자 고갈」).
- ~~해일군주 대체 획득 경로~~ ✅ 완료 — 여명의 섬 조수 제단(`@` 12,1). **NO_WILD 3종 전부 보스로 획득 가능**해졌다.
- **브라우저 테스트 추가 감축** — 규칙 계층이 있으니 순수 로직만 보는 playwright 테스트를
  `rules_unit_test.js`로 이관 가능. 단 `applyStatus`처럼 계산 뒤 `setMsg()`가 붙은 건 계산/연출 분리가 선행.
- **게임필 폴리시** — 로드맵 2단계 잔여.

---

## 세션 기록 (최신이 위)

### 2026-08-11 (3) — NPC 그림 12장이 들어왔다 · 대조표를 쪼개는 전처리를 새로 만들었다

**유저**: "넣었어" — `art_inbox/npc/`에 파일 하나.

**① 받은 형식이 요청과 달랐다 — 그래서 코드가 맞췄다**
문서는 "원형당 파일 1장"을 요청했는데 실제로는 **12원형 × 3자세가 전부 담긴 대조표 한 장**
(1536×1024, 라벨 인쇄됨)으로 왔다. 📌 **이게 오히려 낫다** — 한 번에 뽑았으니 12종의 화풍이 안 갈린다.
생성 AI에게 12번 따로 시키면 갈린다. → 다시 받지 않고 **쪼개는 쪽을 코드로 만들었다.**

**② `scripts/split_contact_sheet.py` 신설 — 왜 `detect_grid`를 못 썼나**
`detect_grid`는 이미지 **전체**의 행/열 프로파일을 본다. 대조표는 4행이 세로로 겹쳐 있어서
열 프로파일이 4행을 전부 합산한다 → 그룹 경계가 사라진다(실측 **7행 × 5열**로 나왔다).
→ **연결 성분**(행 단위 런 + union-find, scipy 없이)으로 자세를 하나씩 찾는다. 36개가 정확히 잡혔다.

**③ 함정 둘 — bbox로 그냥 자르면 안 된다** (둘 다 화면에 실제로 나왔다)
- **이웃이 딸려온다.** miner 2번 자세의 곡괭이가 3번 자세 bbox를 **3px 침범**(x349~351)해
  3번 시트 왼쪽에 검은 파편이 박혔다. 라벨 알약도 guard 1번 자세 bbox 위 2px에 걸쳤다.
  → bbox가 아니라 **성분 마스크**로 자른다(`crop_masked`).
- **성분 하나만 남기면 부속이 사라진다.** 떨어져 있는 조각이 실측 8개(낚싯대 토막·머리 장식 점).
  → bbox에 **완전히 들어오는** 작은 성분만 같이 살린다. 라벨은 자세보다 넓어 이 규칙에서 자동으로 걸린다.
- 자세를 **여백 48px로 다시 깔아** 저장한다. 겹침이 있으면 `detect_grid`가 1행×2열로 읽어 변환기가 거부한다.

**④ 결과**: 12/12 변환 성공(전부 격자 감지 1행×3열) · `npc_sheet_test` 전 항목 통과 ·
빌드 3.8MB → **3.9MB**(시트 12장 ≈ 180KB). 화면 확인까지 했다 — 오버월드 NPC가 실제로 시트로 그려지고
주인공 픽셀과 결이 맞는다.

**⑤ ⚠️ 문서가 약속한 틴트는 코드에 없다 — 43명이 12가지 외모로 줄었다**
프롬프트 문서 §0은 *"같은 원형끼리는 기존 색 지정(`spr.outfit`/`accent`/`hair`)을 그대로 틴트로 얹어
구분한다"* 고 적었는데, **`_char`의 시트 분기(`src/index.html:2865`)는 그림을 그대로 그리고 바로 `return`한다.**
틴트 코드가 아예 없다. → kid 6명·youth_f 4명이 각각 **쌍둥이**로 보인다.
📌 08-11 오전에 실루엣을 12종 → 34종으로 늘린 작업과 **정면으로 반대 방향**이다. 유저 결정 대기(§NEXT_SESSION).
⚠️ **문서에 적힌 동작을 코드가 한다고 가정하지 말 것.** 이번 건 프롬프트 문서를 쓸 때
   "이렇게 하면 된다"를 적어둔 것이 구현으로 읽혔다. 회귀도 이걸 안 봤다(그릴 때 칸만 봤다).

### 2026-08-11 (2) — NPC 원형 시트 경로를 넣었다 (아트 도착 전에 미리)

**유저**: "2번 가자."

**① 47장이 아니라 12장으로 간다**
NPC는 47명인데 원형은 **12종**이면 덮인다(kid·bugkid·youth_f·youth_m·hunter·angler·
miner·scholar·mystic·guard·merchant·elder). 같은 원형끼리는 기존 색 지정(`spr.outfit`/`accent`/`hair`)이
달라서 서로 다르게 보인다. 실제 매핑은 **43명**(나머지 4명은 실내 인물이라 범위 밖).

**② 아트가 오기 전에 코드를 먼저 넣었다 — 그리고 그게 검증 가능하다**
- `NPC_ARCH` — id→원형 **단일 출처**. NPC 정의마다 적으면 43군데로 흩어져 새 NPC를 넣을 때 조용히 빠진다.
- `_char` **맨 앞**에 시트 분기. ⚠️ 호출부가 7군데라 호출부마다 넣으면 한 곳을 빠뜨렸을 때
  "어떤 사람만 옛 그림"이 된다. 한 곳에만 둔다.
- 주인공과 **같은 함수**를 쓴다(`heroSheetCell`/`heroSheetFrame`/`heroDrawSpec`).
  NPC용으로 따로 계산하면 칸 판정이 두 벌이 되어 갈라진다.
- `build.py`에 `npc_sheet` 임베딩. manifest에 없으면 빈 객체 → **빌드가 안 깨진다.**
- `scripts/make_npc_sheet.py` — 12종 일괄 변환. 변환 로직은 `make_hero_sheet.py`를 import해 재사용한다.

**③ 회귀 `npc_sheet_test` — 아트가 없는데 어떻게 검사하나**
시트가 하나도 없으니 새 경로가 아예 안 돈다 → **런타임에 더미 시트를 주입해** 그리기 경로를 태운다
(주인공 시트 때와 같은 방식). 아트 없을 때의 폴백 · 주입 시 올바른 칸(행 0/1/2, 정지 0열) ·
왼쪽만 좌우 반전까지 본다.
📌 **바로 버그를 하나 잡았다** — 문서의 원형표를 코드로 옮기며 `assistant`라는 **없는 id**를 적었다.
   실제 id는 `mina`였다. 화면만 봤으면 "그 사람만 옛 그림"으로 영영 몰랐을 종류다.
⚠️ `NPC_SHEET_ART`는 스코프 안의 const다 — `window`에 새로 만들면 게임은 안 본다.
   SG로 내보낸 **같은 객체**에 키를 꽂아야 실제 경로가 읽는다(테스트가 여기서 한 번 헛돌았다).

**④ 프롬프트 — 처음 짠 걸 고쳤다**
3×3 시트를 요구하는 프롬프트를 썼다가 **파이프라인과 안 맞아 고쳤다.**
변환기는 9칸 시트를 받는 게 아니라 **자세 3개가 담긴 이미지 한 장**을 받아 걷기 프레임을
직접 합성한다(`step_frame`). 주인공 4명이 그렇게 만들어졌고, 생성 AI에게 9칸 격자를 맞추라고 하면
거의 실패한다. → `outputs/production/2026-08-11_spirit-grove_npc-sheet-prompts.md`

**⑤ 지금 상태**: 그림 12장만 오면 **코드 수정 없이** 들어간다. 한 장씩 넣어도 되고, 없는 원형은 폴백한다.

⚠️ **`PW_FROM` 번호는 `pw` 호출 순번이다 — 로그의 테스트 이름과 다르다.**
   부하 위양성으로 멈춘 지점부터 이어 돌리려다 106을 넣었는데, `pw` 호출은 **총 98개**라
   대부분을 건너뛰고 2개만 돌고는 "🎉 모든 검증 통과"가 떴다. 초록이 초록이 아니었다.
   → 번호는 이렇게 확인할 것:
   ```bash
   awk '/^  pw /{n++; print n": "$2}' scripts/verify.sh | grep <테스트이름>
   ```

### 2026-08-11 — 길에 사람을 다시 깔았다 + NPC 실루엣을 갈랐다

**유저**: "1번부터 가자. **npc 외형도 좀 개선해**"

**① 거리만 늘린 건 절반이었다**
어제 체육관을 동쪽으로 옮겼는데 **트레이너는 옛 체육관 자리(서쪽)에 그대로** 있었다. 재보니:

| | 체육관1까지 | 길목 트레이너 |
|---|---|---|
| 리오 | 20보 | 1명 |
| **미나** | 10보 | **0명** |
| **토리** | 19보 | **0명** |
| 엘 | 23보 | 2명 |

거리만 늘리면 "차근차근"이 아니라 **빈 들판이 길어진 것**이다.
(⚠️ 체육관 사이 3구간은 멀쩡했다 — 구간당 3.7명. 구멍은 첫 구간 하나뿐이었다.)

**② 트레이너 3명을 동쪽 척추로 옮겼다**
넷이 공통으로 지나는 칸은 **마을 y48 통로(x16~22)** 다. 거기 시야가 닿게 세웠다.

  초보 정령사 준 (6,46)→(16,47) · 벌레소년 메아 (6,44)→(18,47) · 물놀이 소년 하리 (20,42)→(21,49) 위를 봄

결과: **넷 다 3명씩**, 전부 우회 +0(최단 경로 위), 첫 전투는 5·12·13·16보째.

**③ 중간에 나온 것 둘**
- 트레이너 전투는 **시야(`dir`+`range` 2)** 로 걸린다 → 재야 하는 건 트레이너가 선 칸이 아니라
  **그가 보고 있는 칸**이다. `onboarding_test`가 트레이너 칸만 봐서
  "리오가 **2보째**에 전투"를 놓치고 있었다(준의 시야가 리오 출구 1칸 앞까지 닿았다).
- `dir`을 붙이는 일괄 루프가 `n.dir="down"`으로 **무조건 덮어써서** 개별 방향을 줄 수가 없었다.
  정의에 `dir`을 적어도 조용히 down이 되던 상태 → `n.dir=n.dir||"down"`으로 고쳤다.
- ⚠️ **(22,47)은 아래 (22,48)에서만 닿는다** — 거기 NPC를 두면 체육관이 통째로 막힌다. 자리를 고를 때 확인할 것.

**④ NPC 외형 — 색은 다양성이 아니다**
재보니 47명 중 **33명이 `모자없음+소품없음` 아니면 `캡만`** 이었다. 덩치·윤곽이 전부 같고 옷 색만 달랐다.
렌더러에 축 넷을 넣었다: **체형(adult/child/stout/tall)** · **머리 길이** · 모자 2종(두건·비니) · 소품 2종(등짐·바구니).

| | 전 | 후 |
|---|---|---|
| 실루엣 조합 | 12종 | **34종** |
| 같은 실루엣 최대 무리 | **33명** | 3명 |
| 쓰이는 체형 | 1종 | 4종 |

📌 머리 비율은 안 건드렸다 — 머리만 키우면 눈·입 위치가 따로 놀아서 오히려 망가진다.

**⑤ 회귀**
- `pace_test [5]` — 길목 트레이너 ≥2명 · 첫 전투 ≥5보. **시야 칸**으로 잰다.
  ⚠️ 트레이너 0명일 때 "첫 전투 없음"이 초록으로 새던 **공허한 통과**를 막았다(이 저장소에서 세 번째다).
- `npc_sprite_test` 다양성 절 — 조합 ≥25종 · 같은 실루엣을 쓰는 **서로 다른 인물** ≤4명.
  ⚠️ 이름으로 묶는다 — 라이벌 카이는 4곳에 나오므로 안 묶으면 늘 빨개진다.
- 둘 다 **되돌린 빌드에서 빨개지는 것까지 확인**했다.

**⑥ 덤으로 잡은 플레이크 — `hero_sheet_test`가 무작위로 빨개지고 있었다**
전체 검증에서 "걷는 동안 프레임이 바뀐다 (관측된 열: 0)"로 실패했는데, 같은 빌드를 다시 돌리니
**3/3 통과**했다. 처음엔 표본이 짧아서인 줄 알고 틱을 40으로 늘렸지만 여전히 실패했다.
진짜 원인은 **세로로 걸을 공간이 한 칸뿐**이라는 것이었다 — 출신지 실내는 세로가 짧아
아래로 한 칸 가면 벽이고, 그 뒤엔 `moving`이 false라 정지 프레임(0열)만 계속 나온다.
프레임 순서 `[정지,A,정지,B]` 중 절반이 0열이라 특히 잘 속는다.
→ **열(프레임) 판정을 가로 열린 줄(x=1~7, 6칸)로 옮겼다.** `heroSheetFrame`의 열 계산은
  방향과 무관한 공통 로직이라 단정이 안 약해진다(방향별로 다른 건 **행**이고 그건 그대로 확인한다).
→ 옮긴 자리엔 **"여러 칸 실제로 걸었는가(≥3칸)"** 전제를 붙여 같은 플레이크가 따라오지 못하게 했다.
→ 5/5 안정 확인 · 걷기 애니를 죽인 빌드에서 여전히 잡히는 것도 확인.
📌 교훈: 무작위로 실패하는 회귀는 **표본을 늘려서** 고치는 게 아니라 **왜 조건이 성립 안 하는지**를 봐야 한다.

### 2026-08-10 (2) — 지도의 폭을 실제로 쓰게 만들었다 · 병렬 좌표 테이블 5개 제거

**유저**: "게임 초반에 너무 나오자마자 배틀하는거 좀 별로야. 포켓몬처럼 차근차근 가는 맛이 있으면 좋겠어."
→ 이어서 "**더 다듬고 더 공간을 잘 활용하도록 해. 너무 나오자마자 모든게 진행돼.**"

**① 원인은 느낌이 아니라 배치였다 (실측)**
지도는 가로 25칸인데 **체육관 4개가 전부 `x=8` 한 줄**에 세로로 늘어서 있었다.

| | 옮기기 전 | 옮긴 뒤 |
|---|---|---|
| 체육관 x좌표 | 8 · 8 · 8 · 8 (폭 **0칸**) | 22 · 20 · 2 · 18 (폭 **20칸**) |
| 체육관 사이 | 10 · 11 · 11보 | **22 · 31 · 21보** |
| 출신지→첫 체육관 | 리오8 미나10 **토리3** 엘7 (합 28) | 리오20 미나10 토리19 엘23 (합 **72**) |

⚠️ **눈으로는 절대 안 잡힌다.** 지도를 열어봐도 "체육관이 네 개 있네"로 보인다. 재야만 보인다.

**② 첫 체육관은 (8,43) → (22,46)**
토리가 집에서 **3보**면 문 앞이던 게 19보가 됐다. 게다가 **문 앞 칸이 풀숲(T)** 이라
**야생을 거치지 않고는 관장에게 못 걸어 들어간다** — Lv5 스타터로 직행하는 흐름이 구조적으로 막혔다.
지역·야생 하한은 그대로다(1/2/4/5, 하한 7/13/23/28) — 이름과 난이도 곡선이 안 어긋난다.

**③ 진짜 수확: 좌표를 또 들고 있던 곳이 5군데였다**
지도와 `GYM_AT`을 고쳤는데도 계속 빨간 게 나왔다. 전부 **같은 병렬 테이블 버그**다.

| | 어디 | 증상 |
|---|---|---|
| 1 | `GYM_ORDER` (src) | 바로 위 주석이 "⚠️ 좌표를 상수로 박지 않는 게 이 트래커의 원칙"이라 적어놓고 **본인이 박고 있었다.** 목표 트래커만 옛 자리를 가리켰다 |
| 2 | `goal_test.js` | 지도·트래커가 다 맞는데 **테스트만** 빨갰다 |
| 3 | `gym_test.js` | `{x:8,y:44}`에서 위를 눌러 입장 → `indoor=null` |
| 4 | `indoor_move_test.js` | 같은 원인 |
| 5 | `grid_move_test.js` | "부드럽게: 1칸 이동 (43→43)" — 이동 로직은 멀쩡했다 |

📌 **교훈: 테스트가 든 상수도 병렬 테이블이다.** 전부 `GYM_AT`에서 파생하게 바꿨다.

**④ 회귀 `pace_test`** — 이 간격이 다시 무너지지 않게 못 박았다.
출신지→첫 체육관 ≥7보 · 체육관 사이 ≥15보 · x좌표 폭 ≥12칸 · 첫 체육관 문 앞이 풀숲.
⚠️ **되돌린 빌드(체육관을 다시 x=8 한 줄로)에서 10개 단정이 전부 빨개지는 것까지 확인**했다.

**⑤ 앞서 끝낸 공간 재배치도 함께**
- 반딧불길 입구 (21,44)→(23,47): 양 끝이 둘 다 초원이 돼 **마을↔초원이라는 목적을 잃었던 것**을 되돌렸다.
  회귀 `route_test [1-c]`가 "각 지선이 서로 다른 지역을 잇는가"를 본다(잔교만 의도적 예외).
- 정령회관 (13,45)→(12,34): 가장 빽빽하던 초원 → 숲. 밀도 초원 1.5→1.17/행, 숲 0.71→0.86/행.

**⑥ 측정 중 틀린 것** — `GROUND_ITEMS`를 `{"x,y":…}` 맵으로 착각해 "초원·마을 아이템 0개"라는
잘못된 결론을 냈다. 실제로는 **배열**이고 마을1·초원4개가 있다. 구조를 먼저 확인하고 셌어야 했다.

### 2026-08-10 — 출신지 인물 + 신규 정령 설계 + fullrun 판정을 접었다

**① 출신지 인물 4명** — 출신지 4곳을 만들 때 지도·도입은 넣었는데 **안에 있는 사람은 비워뒀다**.
폴백이 걸려 네 곳 모두 "집주인"이 같은 말을 하고 있었다. 소장·동행 상인·할아버지·숲의 목소리로 채웠다.
화자 이름도 대사 문자열에서 뽑게 해 단일 출처로 만들었다(예전엔 상점 아니면 전부 "집주인" 하드코딩).
회귀 `origin_npc_test` — 📌 핵심 단정은 **집에 있는 사람 = 도입에서 배웅한 사람**이다.
각각 보면 둘 다 멀쩡해 보여서, 대조하지 않으면 어긋나도 모른다.
⚠️ 내가 낸 사고: 주석에 정규식을 적었더니 그 안의 슬래시-별 조합이 **블록 주석을 일찍 닫아 빌드가 통째로
깨졌다**(`window.SG`가 사라짐). 문법 검사로 잡았고 같은 자리에 경고를 남겼다.

**② 신규 정령 4종 설계**(아트 대기) — 81종을 훑어 구조적 공백 둘을 찾았다.
**독 주타입 최종진화가 0개**(10타입 중 유일) · **t2에서 끊기는 라인이 10개**(합 134~168에서 멈춘다.
3단은 175~205까지 간다 → 취향이 아니라 손해다). 희소한 타입부터 끊긴 라인에 최종형을 하나씩 붙인다.
설계: `outputs/production/2026-08-10_spirit-grove_new-species-design.md`
⚠️ 아트는 **픽셀이 아니라 페인터리**로 받는다 — 기존 86종이 페인터리 원본을 변환한 것이라
같은 변환기를 통과해야 화풍이 맞는다.

**③ 📌 fullrun 완주 검증을 접었다(유저 결정).**
두 판 다 브라우저가 환경 압박으로 죽었다(55.3분 뱃지4/4 · 36.0분 뱃지3/4, **둘 다 런타임 에러 0**).
08-05에도 43.8분에 죽었다 → **08-06 완주가 운 좋은 판이었다.**
⚠️ 2차는 부하가 더 낮았는데(0.22 vs 0.8) **오히려 일찍 죽었고** 체육관2는 기준보다 **빨랐다** →
"느려졌다 = 회귀"로 읽으면 안 된다. 편차가 그냥 크다.
→ 맵 수정이 위협하는 건 완주 시간이 아니라 **사슬이 끊기는가**다. `goal_reach_test`·`lord_route_test`·
`route_test`·`gate_test`·`door_front_test`·`origin_test` 여섯 개로 대신한다. 몇 분이면 끝나고 결정적이다.
⚠️ **내 판단 착오 하나**: 유저가 "3번과 1번 동시에"라고 해서 병행했는데, 그게 1차 측정을 오염시켰다.
브라우저를 안 띄워도 node 스크립트는 CPU를 먹는다. **fullrun은 단독으로만 돌린다.**


### 2026-08-07 — 아트가 통째로 픽셀이 됐고, 지도와 시작이 손봐졌다

커밋 9개. 유저가 직접 플레이하며 짚은 것들을 순서대로 처리했고, **매번 화면을 찍거나 전수로 재서** 확인했다.

**① 아트 픽셀 전환** — 주인공 4명(걷기 시트 3×3) + 정령 86종. 빌드 **3.9MB → 1.2MB**.
📌 **아트만 바꾸면 안 됐다.** 확대되는 곳마다 보간이 켜져 있어 전부 뭉갰다(네 곳을 최근접으로).
도구 둘을 남겼다: `make_hero_sheet.py`(배경 제거·그림자 제거·도트화·걸음 합성) ·
`pixelize_creatures.py`(원본은 `art_inbox/`로 옮기고, 입출력이 같으면 거부).
⚠️ 그림자는 **색이 아니라 알파로** 가른다(실측: 그림자 알파 100~140, 몸 255). 축소 **전**에 걸어야 한다.

**② 출입구가 안 보이던 것** — 유저가 여러 번 제보했는데 여태 안 고쳐진 이유가 나왔다.
07-28에 건물을 **정면 진입만** 가능하게 바꿔놓고(`da923b6`) **그 규칙을 알려주는 시각 신호를 안 넣었다.**
안내라곤 틀린 방향으로 부딪혔을 때 뜨는 텍스트뿐이라 **실패한 뒤에야** 알 수 있었다.
🐛 게다가 **`$`(지선 루트 입구)는 표시 목록 어디에도 없어 통째로 빠져 있었다** — 유저가 말한
"집에서 왼쪽으로 갔을 때 갑자기 들어가진다"가 정확히 그 자리다.
→ 회귀 `door_front_test [7]`이 **맵에 있는 입구 타일이 전부 표시 대상인지** 자동으로 잰다. 사람이 기억 안 해도 된다.

**③ 📌 `walkable`을 지형/점유로 쪼갰다.** 한 함수가 둘을 섞어 봐서 같은 함정을 두 번 밟았다
(관문 기둥이 NPC를 따라 춤춤 · `door_front_test [5]` 간헐 실패 — 재스캔 4회로도 못 막았다.
NPC가 45% 확률로 제자리에 머물러 3초를 버틴다). **구조를 묻는 단정은 `terrainWalkable`을 쓴다.**

**④ 건물 확대** — 가로 72%·세로 54%였다. **위로 늘리면 안 됐다**: 문 20곳 중 **16곳이 위 칸이 걸을 수 있는
풀밭**이라(실측) 덮으면 "갈 수 있는데 막힌 것처럼" 보인다 → 접지선 고정으로 1.45배.
블록 7개를 각각 고치지 않고 **바닥을 칠한 뒤 좌표계를 한 번 바꿔** 전부 같이 키웠다.

**⑤ 목표 표식** — 트래커 박스엔 방향·거리가 있는데 지도엔 아무 표시도 없었다.
화면 안이면 고리+핀, 밖이면 가장자리 화살표+칸수.

**⑥ 시작 페이싱 + 출신지 4곳** — 집 문에서 두 칸이 풀숲이었고 집 옆이 루트 입구였다.
풀숲을 물리고 입구를 옮긴 뒤, **캐릭터마다 다른 출신지**를 넣었다(연구소/야영지/농가/빈터).
⚠️ **유저 원안(오버월드 지역을 나눈다)은 불가능했다** — 난이도가 y 하나로 결정되는데 스타터가 Lv5라
숲(Lv13) 이상에서 출발하면 즉사한다. 지도 재설계는 별개 공사라, 출신지를 인테리어로 주고
**서로 다른 지점으로 내보내는** 방식으로 성립시켰다(설계: `outputs/strategy/2026-08-07_...four-origins-design.md`).
줄기 사실은 넷 다 같고 **누가 어떻게 알게 됐는가**만 다르다 — 리오는 편지를 **쓴 쪽**이라 관측 기록으로 시작한다.

**⑦ 회귀 3종 신설** — `hero_sheet_test`(캔버스 픽셀을 읽어 어느 칸이 그려졌는지 역산) ·
`origin_test`(넷 전부 실제로 굴린다) · `door_front_test [7]`. 전부 **되돌린 빌드에서 실패를 확인**했다.
⚠️ 기존 테스트 2개가 옛 전제를 박아두고 있어 같이 고쳤다 — `opening_test`가 **문구**("아침 햇살"·"엄마")를
단정하고 있어 서사를 바꾸자 빨개졌다 → **역할**을 단정하도록 전환. `blackout_test`는 STARTPOS → HOME_POS.

**⑧ 내 실수 하나** — `git add -A`로 스킬 저장소 2개(415개 파일)를 딸려 커밋했다. 빼내고 `~/Desktop/sa up/`으로 옮겼다.
⚠️ 이 저장소에서 `git add -A` 전에는 `git status`로 새 디렉토리부터 확인할 것.


### 2026-08-06 (4) — 주인공 아트: 픽셀 전환 준비 (코드 선행)

유저: "주인공들 너무 이질감 들어. 2D, 픽셀로 만들 수 있게끔 프롬프트 짜줘"

**① 프롬프트를 짓기 전에 화면을 띄워 원인부터 봤다.** 오버월드 한 화면에 화풍이 **넷** 섞여 있다:
주인공(애니 일러스트+베이지 배경판) · NPC/트레이너(**코드가 그리는 픽셀**) · 정령(수채 페인터리) · UI(이모지).
📌 **결정적 사실 — NPC는 이미 픽셀이다.** 주인공만 밖으로 튄다. 그래서 "주인공을 픽셀로"가 옳은 방향이다.
지금 아트의 결함 4가지: 배경판 잔존(`hero/0`·`hero_back/0`) · 4종이 서로도 불일치(`hero/1`엔 판 없음) ·
발이 캔버스 아래 끝에 안 닿아 **떠 보임** · 코드가 그리는 타원 그림자와 **이중 그림자**.

**② 코드 전제 2가지를 실측으로 찾았다.**
- `imageSmoothingEnabled`가 **소스 어디에도 없었다** = 기본값 true → 픽셀 아트를 넣으면 그대로 뭉개진다.
- 주인공 그리기 크기가 32의 정수배가 아니다(실측 75/67/81px) → 도트 격자가 울퉁불퉁해진다.

**③ 📌 그냥 둘 다 켜면 오늘이 나빠진다** — 고해상도 일러스트를 스무딩 끄고 축소하면 거칠어진다.
→ **원본 크기 하나로 갈리게** 만들었다: `heroDrawSpec(srcW,ts)`(`src/rules/util.js`, DOM 없는 순수 함수).
원본 ≤64px면 픽셀 취급(32 정수배 **내림** + 최근접), 크면 지금 그대로(ts*0.98 + 보간).
**오늘 아무것도 안 바뀌고, 32×32 webp를 넣는 순간 자동으로 넘어간다.** 플래그 데이가 없다.
⚠️ 반올림이 아니라 **내림**인 이유: 반올림하면 뷰포트별로 64↔96을 오가 크기가 50% 튄다(타일 실측 68~83px).
⚠️ 캐릭터 선택 미리보기(`charPreview`)도 **같은 함수**를 쓴다 — 따로 계산하면 선택 화면과 실제가 갈린다.

**④ 검증**: `rules_unit_test`에 8단정 추가(브라우저 불필요). 게다가 **32×32 체커보드를 실제로 주입해 그려
중간색 0.00%** 확인 — 최근접이 진짜로 도는 것까지 봤다(스펙만 맞고 실제로 안 도는 걸 막는다).

**⑤ 절차적 워커 대안을 실제로 만들어 비교했다.** `HERO_ART`를 비우면 코드가 이미 가진 `_walker`로 되돌아간다.
스크린샷 결과 **NPC와 화풍이 정확히 일치하고 배경판·뜸이 즉시 사라진다.** 대신 디테일이 확연히 적다(빌드는 214KB 가벼워짐).
→ **픽셀 아트를 만들 이유는 "일관성"이 아니라 "일관성 + 디테일"이다.** 일관성만 원하면 공짜로 얻는다.

**⑥ 산출물**: `outputs/production/2026-08-06_spirit-grove_hero-pixel-art-prompts.md`
규격표 + **프롬프트 8종**(4캐릭터 × 앞/뒤, 공통 프리픽스·네거티브 포함) + 검수 체크리스트.
색은 지어내지 않고 게임 데이터 `CHARS`의 실제 hex를 썼다(선택 화면 절차적 미리보기가 같은 색을 쓴다).
📌 코드에서 유도한 핵심 제약: 아트는 **2장뿐**이고 왼쪽은 앞모습 좌우 반전이라
**한 장이 "정면"과 "오른쪽"을 겸한다** → 정면에서 오른쪽으로 15~25° 튼 자세가 규격이다.

⚠️ 부하 오탐 2건(`exp_test`·`door_front_test`) — 둘 다 단독 재실행 통과. 해당 구간을 처음부터 다시 돌렸다.


### 2026-08-06 (3) — 포켓몬 갭 2차: 물리/특수가 기술이 아니라 **타입**에 묶여 있었다

**① 먼저 갭이 *아닌* 것을 걷어냈다**(실측 — `window.SG`에서 직접 셈).
타입 상성표는 100칸 중 **45%가 비등배로 본가(약 38%)보다 촘촘하다** · 기술 108개 중 못 배우는 건
`struggle`뿐 · 특성 21종 전부 배선됨 · 노력치·개체값·육아방(개체값/성격/알기술 유전) 전부 있음 ·
진화 조건 4종류. **"강한 스탯 쪽 STAB이 없는 종"은 0마리다.**
⚠️ 중간에 제가 "주 타입 불일치 7종"을 갭으로 잡았다가 **이중 타입까지 세니 0이라 정정**했다.

**② 진짜 갭**: `damage()`가 `SPEC_TYPES[move.type]`으로 계열을 정했다(3세대 이전 방식).
- **모든 타입이 한쪽 계열의 공격기만 갖고 있었다** — 불 물리 0 · 바위 특수 0 …
- **이중 타입 40종 중 31종**이 두 타입이 물리/특수를 가로질러 STAB 하나를 약한 스탯으로 쐈다.
- 그리고 **UI가 계열을 아예 안 보여줬다.** 타이틀 배지엔 "물리·특수"라 적혀 있는데
  게임 안에선 알 방법이 없었다 — 집게왕 하이드로펌프 145 vs 락슬라이드 333의 이유를 못 읽는다.

**③ 고친 것** — `MOVES.cat` 덮어쓰기 + `moveCat()` 단일 출처(`DEFAULT_ABILITY`/`ABILITY_OVERRIDE`와 같은 idiom).
분기점은 `damage()`와 까칠한피부 **2곳뿐**이고 둘 다 `isSpecMove()`를 거친다. UI 3곳에 계열 표기.

**④ 📌 이번 사이클 최대 교훈 — "본가와 같게"가 곧 개선이 아니다.**
처음엔 본가 기준으로 13개를 뒤집었는데, 실측해보니 **여러 개가 그 기술을 배우는 종을 약화시켰다**.
이 게임의 종족값은 타입이 계열을 강제한다는 전제로 짜여 있기 때문이다
(파괴광선을 특수로 돌리면 배우는 **22종의 평균 atk 24.0 > spa 20.8**이라 전부 손해 —
덩굴채찍·얼음뭉치·플레어드라이브·와일드볼트·머드샷도 같은 이유로 **되돌렸다**).
→ 기준을 둘로 좁혔다: **① 배우는 종에게 이득이고 본가와도 맞는 것 ② TM 전용(배우는 종 0 =
트레이너·야생 미사용 = 밸런스 불변)이라 없던 선택지를 만드는 것.** 최종 9개.
⚠️ **새 항목을 넣기 전에 그 기술을 배우는 종의 평균 atk/spa를 먼저 재라**(주석에 박아뒀다).

**⑤ 결과**: 10타입 중 **7타입이 물리·특수를 모두 갖게 됐다**(전엔 0). 남은 셋(normal·grass·rock)은
뒤집을 후보가 전부 "배우는 종 손해"이거나 TM 전용 기술이 아예 없어 **알면서 남겼다** — 회귀가 집합을 고정한다.
죽은 STAB은 집게왕(아쿠아제트로 교체 145→224 + 선제공격) · 화염나방(바람일격 특수화) ·
마그마룡(열창 물리 TM) 셋이 풀렸다. ⚠️ **뇌전매·푸르사는 계열이 아니라 위력 차이라 안 풀린다**(정직하게 남긴다).

**⑥ 회귀 `movecat_test` 신설**(`verify.sh` 등록). **되돌린 빌드 2종으로 실패 확인**:
`damage()`가 타입을 다시 보면 [1]이, **까칠한피부만** 타입을 다시 보면 [3]이 걸린다(서로 다른 단정이 잡는다).
[5]는 "한쪽 계열뿐인 타입" 집합을 **임계값이 아니라 집합으로 고정**한다.

**⑦ 밸런스 재측정** — ⚠️ **종반이 실제로 어려워졌다. 노이즈가 아니다.**
`balance_test` 3000판: 체육관4·숲의 군주는 플레이어 **+1~4%p**, 챔피언은 **-1~4.6%p**.
`league_test` 1500판: Lv54 완주율 35.9%→31.8%(전설 없음) · 53.3%→46.8%(전설 1).
📌 **원인이 명확하다 — 챔피언(과 리그 챔피언 아리엘)의 선두가 집게왕이다.** 죽은 STAB을 고쳤더니
그 종을 쓰는 트레이너가 같이 세졌다. 이 저장소가 반복해서 만나는 그 원칙이다.
단정은 `balance_test`·`league_test` 양쪽 전부 통과하고 「의도된 도전 레벨」은 6게이트 모두 그대로다.
⚠️ **종반을 되돌리고 싶으면 집게왕 기술셋(dex.js)을 원복하면 된다** — 대신 죽은 기술이 돌아온다.

### 2026-08-06 (2) — 묶음 C 배포 + 야생/트레이너 지닌물건 풀 분리

**① 묶음 C 배포.** Actions success · 라이브 HTML에서 신규 5종 전부 확인. `main:history`는 이미 최신이었다.
(세션 끝에 아래 ②까지 **한 번 더 배포**했다 — 라이브에 `TRAINER_HELD`가 올라간 것으로 확증. 미배포분 없음.)

**② 야생 지닌물건 풀 확장 — 유저 판단 항목이었고, 물었더니 「풀을 둘로 분리」를 골랐다.**
- 📌 **문제의 구조**: `WILD_HELD` 하나를 야생과 트레이너가 **공유**하고 있었다(`trainerMon`→`makeMon`).
  그래서 "야생 사냥 유인을 올리려고 풀을 넓히면 트레이너가 같이 세지는" 딜레마가 있었다.
  → 풀을 쪼개니 **딜레마 자체가 없어진다.** 물어본 두 선택지(확장 vs 유지)보다 나은 세 번째 안이었다.
- `TRAINER_HELD`(옛 값 그대로 **동결**) / `WILD_HELD`(신규 5종 편입, tier2에 1종·tier3에 5종).
  휴대율(5%/9%/14%)은 안 건드렸다 — 이번에 바꾼 건 "무엇을 드느냐"뿐이다.
- 트레이너 측 `makeMon` 호출부는 **3곳**이었다: `trainerMon` · `startDouble`의 `DB.foes` · `dbReplace`의 벤치 교대.
  ⚠️ **`trainerMon`만 고치면 듀오 트레이너 2곳이 조용히 야생 풀을 쓴다** — 이 저장소 단골 사고(호출부 누락)라
  회귀를 호출부 단위로 짰다.
- ✅ 회귀 `held_effect_test [5]` 신설. **표를 비교하지 않고 실제 생성 경로를 돌린다**
  (`F.trainerMon` 4710마리 · `F.startDouble`+`F.dbReplace` 240마리). 인자를 빠뜨리면 그 경로만 빨개진다.
  ⚠️ **되돌린 빌드 2종으로 실패를 확인했다**(trainerMon만 되돌린 것 · 듀오 2곳만 되돌린 것) — 공허한 통과 방지.
  - 이걸 위해 `getDB`·`dbReplace`를 `window.SG.flow`에 노출했다(읽기/구동용, 게임 동작 변화 없음).
- 🐛 **부수 효과 하나를 미리 확인했다**: `resolvering`(구애 lock)이 야생 풀에 들어가면서
  **AI가 조종하는 정령이 lock 아이템을 드는 첫 사례**가 됐다. `foeChooseMove`(`index.html:1207`)가
  `_choiceLock`을 이미 존중하고 **PP가 마르면 다른 기술로 빠져나가므로** 교착이 없다. 듀오 상대는
  `TRAINER_HELD`에 lock 아이템이 없어 무관.
- **재측정(트레이너는 불변이지만 플레이어는 아니다)**: `balance_test`·`league_test`가 **플레이어 팀을
  `makeMon` 기본값으로 만든다** → 확장된 야생 풀이 그대로 들어간다. 그래서 그냥 넘기지 않고 쟀다.
  베이스라인(직전 커밋 dist) 대 신규, 각 2000판/1200판:
  게이트 승률 ±1.5%p 이내(부호 혼재) · **「의도된 도전 레벨」 6게이트 전부 동일** ·
  리그 완주율 이동도 노이즈 범위(Lv54 36.4%→35.0%) · 양쪽 단정 전부 통과.
- ⚠️ **부하 오탐 2건**(`gym_ice_test`·`route_test`) — 둘 다 단독 재실행에서 통과. §5의 "목적지 한 칸 차이"
  패턴 그대로다. `set -e` 때문에 뒤 테스트가 안 도니 **그 구간을 처음부터 다시** 돌렸다.

### 2026-08-06 — 완주가 닫혔다 + 성격이 로스터의 72%를 비켜 가고 있었다

**① fullrun 완주 성공**(위 「현재 상태」). 어제 고친 세 가지가 한 판에서 다 작동했다.
⚠️ 부하가 낮을 때(코어당 0.46) 돌렸다 — 이 맥에서 90분 판의 성패는 부하가 가른다.

**② 묶음 A — 성격-타입 나침반 완성**(포켓몬 갭 전략의 1순위. 유저 승인 3건 받고 착수)
- 🐛 **실측: 성격 10개인데 고유 효과는 7개뿐이었다.** `고집있는`=`외로운`, `명랑한`=`겁쟁이`로 **완전 중복**.
  본가에서 이 둘은 `atk↑spa↓`·`spd↑spa↓`인데, **`spa`가 테이블에 없어 down이 다른 스탯으로 대체된 흔적**이다.
  → 배선 누락이 "빠졌다"를 넘어 **기존 2개를 잘못 정의하게** 만들고 있었다. 유저 승인으로 본가 정의로 복원.
- 📌 **왜 갭인가**: 타입 10개가 `SPEC_TYPES`로 **물리 5 : 특수 5**로 정확히 이등분돼 있고 크리처에 `spa`/`spDef`가
  있는데 성격 보정은 `atk`·`def`·`spd`에만 있었다 → **주 타입이 특수인 62/86종(72%)에게 성격이 사실상 무의미.**
  "포켓몬에 있으니 넣자"가 아니라 **이미 설계한 물리/특수 분리를 성격이 절반만 따라간 것**이다.
- ✅ 성격 **10 → 16**(고유 효과 **7 → 14**). 추가: 조심스러운·의젓한·냉정한(spa↑)·차분한·건방진(spDef↑)·장난꾸러기(spDef↓).
  ⚠️ `recalc`가 `m[nat.up]*=1.1`이라 **코드 변경 0** — `src/rules/tables.js`만 고쳤다.
- ✅ **닿게 만들었다**: 민트 5종이 `atk↑·def↑·spd↑`만 덮고 있어 신규 `spa↑·spDef↑`는 뽑기 운뿐이었다
  → `조심 민트`·`차분 민트` 추가(필드·상점·`ITEM_KO` 3곳). `NATURES`를 `window.SG`에 노출(테스트용).
  ⚠️ **`고집 민트` 설명이 `(공↑방↓)`으로 실제와 어긋나 있었다** — 표를 고치자 드러난 병렬 테이블 사고.
- ✅ **회귀 2종 신설**(둘 다 **수정 전 빌드에서 실패 확인**):
  `rules_unit_test` 「성격 테이블 위생」 6단정(다섯 스탯이 up·down에 전부 등장 · 중복 효과 없음 ·
  spa 성격이 실제로 특수공격을 올림) · `dead_content_test` 2단정(다섯 스탯 전부 민트로 구매 가능 ·
  **민트 설명이 성격 표와 일치**).
  ⚠️ 처음엔 `NATURES`가 `window.SG`에 없어 민트 설명 단정이 **빈 배열을 돌며 공허하게 통과**했다 — 노출로 해결.
- ✅ **밸런스 재측정**(유저가 "재측정 필요" 분류를 승인): 게이트 승률 이동은 노이즈 범위,
  리그 완주율 13.3% → 14%, 리그 단정 7건 전부 통과. ⚠️ **불변이 아닌 이유**: `makeMon`이 야생·트레이너·알을
  전부 공유해 새 성격이 **트레이너 로스터 생성에도 섞인다**. 「플레이어 전용=밸런스 불변」 통념의 예외다.

**③ 묶음 B — 목표 트래커 도달성 감사**(`scripts/goal_reach_test.js` 신설, verify.sh 등록)
핀포인트 회귀(`lord_route_test`는 군주 한 지점만 본다)를 **사슬 전체**로 일반화했다.
체육관1~4 · 호수 제단 · 군주의 제단 · 리그 · 도감 완성 + 진행 상태 20가지 전수.
**게임의 실제 클릭 핸들러를 그대로 부른다**(병렬 로직 금지). 08-05의 두 사고를 되돌린 빌드로
**실제로 잡는 것까지 확인**했다(캐시 버그 → [4]가, 물길 제거 → [1]이 잡는다).
⚠️ 짜면서 밟은 함정 3건(전부 픽스처였다): 클릭 후 기다렸더니 spotTrainer가 경로를 지웠다 ·
   트레이너 배제에 "X"를 넣어 **검사 대상인 군주 단계를 픽스처가 지웠다** ·
   [4]를 같은 페이지에서 돌려 앞 구간이 채운 캐시 때문에 **버그 빌드가 초록**이었다(새 페이지로).

**④ 묶음 C — 지닌물건 19 → 24종**(전투 코드 변경 0)
착수하자마자 **`lifeorb`의 `recoil:0.1`이 아무 데서도 안 읽히고 있던 것**이 나왔다 —
실제 반동은 `held==="lifeorb"` 키 비교 + `maxHp/10` 하드코딩이라 표의 필드는 장식이었다.
반동 아이템을 새로 넣어도 조용히 아무 일도 안 일어난다 → 단일 출처로 일반화(메시지 이름도 함께).
신규 5종은 **기존 effect 필드 조합만** 쓰고 전부 상점 판매 = 플레이어 전용 = 밸런스 불변(재측정 통과).
✅ 회귀 `held_effect_test` — **표에 선언한 필드마다 관측 가능한 차이를 단정**한다. 하드코딩으로
   되돌린 빌드에서 실패 확인(실측 감소 30 vs 기대 50).
⚠️ **`dex.js` 주석이 틀렸었다**: `trainerMon`이 `makeMon`을 그대로 써서 **트레이너 정령의 10.8%가
   야생 풀 지닌물건을 든다**. "트레이너는 덮어쓴다"는 서술은 사실이 아니었다 →
   `WILD_HELD`를 건드리면 트레이너가 같이 세진다(성격의 makeMon 공유와 같은 부류).

**⑤ `lord_route_test [8]`의 단정이 상태에 오염돼 있었다**(어제 내가 넣은 것) — 물길을 건넌 **직후**라
`G.surfing=true`인 채로 "뭍길 우선"을 쟀다. `walkable`은 파도타기 중이면 물을 통행 가능으로 돌려주고
**로밍 NPC 점유까지 보므로**, NPC가 뭍길을 막은 판에서 BFS가 물로 우회해 실패했다.
→ `surfing=false`로 놓고 잰다. ⚠️ **어제 배운 두 함정(상태 조립 위치 · walkable의 NPC 포함)이 한 줄에서 겹쳤다.**

⚠️ **알려진 노이즈**: `balance_test`의 「모든 게이트에 해볼 만한 구간」 단정이 **체육관1에서 가끔 실패한다**
(Lv8 ≈57% · Lv10 ≈85%로 구간 경계에 걸쳐 있어 노이즈가 밀면 빈다). 기준선 3판·변경 후 4판 모두 통과했고
Lv8 승률도 58% vs 57.3/57.5%로 동일 — **이번 변경과 무관한 기존 취약점**이다. verify.sh에는 없다.

### 2026-08-05 — 어제 고친 게 봇이 아니었다: `findTile` 캐시가 결말 두 개를 지우고 있었다

유저 "가자" → 미완이던 fullrun 완주 재검증. 한 판 돌렸더니 **어제와 똑같이** `숲의 군주 미격파 ·
챔피언 등극`으로 끝났다(43.0분 · 챔피언 2581초 · 런타임 에러 0). 어제 봇 수정(`400335f`)이 안 먹은 게 아니라
**애초에 실행될 기회가 없었다** — 봇은 트래커를 따라가는데 **트래커가 제단을 가리킨 적이 없었다.**

- 🐛 **원인: `findTile`의 영구 null 캐시** (게임 버그, 봇 무혐의). 세 가지가 겹쳤다:
  ① `currentGoal()`은 뱃지<4면 **체육관 단계에서 리턴**한다 → `%`·`X`는 그때까지 **한 번도 조회되지 않는다.**
  ② 그래서 두 문자의 **최초 스캔이 4번째 뱃지를 따는 순간 = 항상 체육관 안**에서 일어난다.
  ③ 실내에서는 `MAP_STR`·`W`·`H`가 인테리어 지도라 못 찾고 → `_tileCache[ch]=null`이 **세션 내내** 남는다.
     `currentGoal`의 `if(alt)`·`if(lord)` 가드가 조용히 통과 → **4뱃지 → 곧장 리그**.
- 💥 **어제 작업 두 건이 실플레이에서 통째로 무효였다** — 서사 3단계의 핵심(`9d5ba1d` 진짜 결말을 사슬 안으로)과
  리그 절벽 완화(`aa43c50` 전설 준비 단계). **봇만이 아니라 모든 플레이어에게** 그랬다.
- 🔍 **왜 아무도 못 잡았나** (이 저장소의 반복 패턴이 또 다른 옷을 입었다):
  · **리로드하면 캐시가 비어 증상이 사라진다** → 손으로는 재현이 거의 안 된다.
  · **`lord_route_test`가 구조적으로 못 잡는 테스트였다** — 상태를 **야외에서** 조립하고 트래커를 처음 읽어
    캐시가 올바른 값으로 채워진다. 어제 이 테스트가 초록이라 "게임 쪽 경로는 확정"이라고 적었는데 **틀렸다.**
    ⚠️ 교훈: 픽스처가 **상태를 어디서 조립하느냐**가 커버리지를 바꾼다. 순서 의존 캐시는 순서를 재현해야 잡힌다.
  · 런타임 에러 0 · 완주까지 초록 · 요약의 "군주 미격파" 한 줄만이 신호였다(어제와 동일).
- ✅ **수정**(`src/index.html` `findTile`): 실내면 **`_owBackup`(오버월드 원본)** 을 스캔한다 + **null은 캐시하지 않는다**
  (스캔은 25×49라 비용이 없다 — 한 번 잘못 잡으면 세션 내내 못 고치는 게 이 사고의 본질이었다).
- ✅ **회귀 신설**: `lord_route_test` **[7]** — **실내에서 4뱃지가 되는 순간**을 재현한다.
  ⚠️ 수정 전 빌드에 돌려 **4건이 정확히 실패**하는 것을 확인했다(이 저장소가 밟은 "공허한 통과" 방지).
  관련 회귀 5종(goal·altar·gate·opening·reachability) 전부 통과.
- 🧹 **환경**: 로드 8.3/8코어의 정체는 **부모가 죽은 고아 headless_shell 6개**였다. `pkill -f headless_shell`로 해소.
  ⚠️ 테스트 전 습관으로 굳힐 것.

#### 🐛 게임 버그 ②: 물로 둘러싸인 목표는 자동 이동이 아예 안 됐다 (①을 고치니 드러났다)
①을 고치자 트래커가 처음으로 **호수 제단(%)** 을 가리켰는데, 봇이 4뱃지 직후 **(8,18)에서 27분간 얼어붙었다.**
`%`는 사방이 물인데 트래커의 대체 경로 탐색이 **상하좌우 보행칸만** 봐서 후보가 0개 →
`"지금은 그곳까지 가는 길이 막혀 있다"`를 띄우면서 **동시에 그곳을 가리켰다.** 봇은 힌트를 안 읽으니 그냥 서 있었다.
- 📌 실측: 호숫가 76곳 중 64곳에서 물길이 제단까지 이어진다 → **손으로는 갈 수 있는 콘텐츠**였다. 자동 이동만 못 갔다.
  (전설 준비 단계는 07-29에 넣었지만 ① 때문에 사슬에 뜬 적이 없어 **한 번도 실플레이된 적이 없었다.**)
- ✅ **수정**: `bfsPath(sx,sy,tx,ty,surf)` — surf면 파도타기를 배웠을 때 물을 통행 가능으로 본다.
  물가에 닿으면 **기존 "물에 부딪히면 파도타기 시작" 분기가 그대로** 발동하고, 연출이 지운 경로는 이어 붙인다(`_surfResume`).
  ⚠️ **뭍길을 먼저 다 시도하고 없을 때만 물길을 연다** — 처음부터 열면 BFS가 최단거리를 골라
     **체육관 가는 길이 호수를 가로지르는 지름길로 조용히 바뀐다.** 이 순서를 회귀 [8]이 단정으로 강제한다.
  ⚠️ `walkable()`(충돌 판정)은 안 건드렸다 — 거기서 물을 통과시키면 연출도 `G.surfing`도 없이 물 위를 걷는다.
- 실측: 트래커 탭 **한 번**으로 호숫가까지 걷고 → 자동 파도타기 → 물 4칸 → 제단 옆(16,22) 도착.

#### 봇 하네스 (게임 무혐의)
- **정체 감지 신설**(유저 승인) — 27분 정지가 "런타임 에러 0"으로 **초록으로 끝났다**. 그게 이 하네스의 유일한 사각이었다.
  ⚠️ 지문 설계에서 두 번 틀렸다: (a) 전투수·레벨을 넣었더니 **문 앞 그라인딩**에 매번 바뀌어 안 잡혔다.
     (b) 좌표만 봐도 안 된다 — 그라인딩은 풀숲을 오가서 좌표가 계속 변한다.
     → **"목표 2칸 이내인데 N분간 못 들어감"** 을 직접 잰다(`POS_STALL_MIN`, 기본 12분). 이게 실패의 실제 모양이다.
- **부딪힘 카운터가 영구적이었다** — 문 앞에서 13회 실패하면 그 판 내내 다시 안 시도한다.
  3차 판이 군주의 제단 정면(8,9)에서 **46분을 그라인딩만** 한 원인. → 45초마다 재시도.
- ⚠️ **게임은 무혐의다**: 같은 좌표·같은 상태(파도타기 중 포함)로 ArrowUp을 주면 제단에 들어간다(단독 프로브 + `[3]`).

#### 테스트 노이즈 2건 (원래 있던 것 — `set -e`가 가리고 있었다)
`verify.sh`는 첫 실패에서 멈춘다 → 아래 둘째 건은 첫째 건 뒤에 숨어 **한 번도 돌지 않았다.**
- `twoturn_test`: 날기 명중 95라 **약 5% 확률로 전체 verify가 빨개졌다**(같은 빌드가 통과/실패).
  시뮬 블록은 `Math.random` 고정, 실전투 블록은 재시도. 12판 연속 통과 확인.
- `door_front_test`: `walkable()`이 **로밍 NPC 점유까지** 봐서 "문 앞에 NPC가 서 있음"을 "지도가 막힘"으로 읽었다.
  → NPC가 움직일 시간을 주고 여러 번 재서 **매번 막힌 문만** 남긴다.

### 2026-08-04 (2) — fullrun 3판: 봇이 제단을 그냥 지나가고 있었다
유저 "한번 돌려보자" → 트래커 사슬을 바꿨으니 봇 완주로 검증하려 했다. 결과는 **결함 1건 + 미검증 1건**.

- 🐛 **봇이 제단을 목표로 인식하지 못했다**(`400335f`) — 봇의 "목표 자체인 인테리어" 목록이
  `gym*`·`league`뿐이라 제단에 들어가면 일반 탈출 분기로 떨어져 **곧장 걸어 나왔다.**
  1차 판이 `숲의 군주 미격파 · 챔피언 등극`으로 끝난 게 그 증거다.
  ⚠️ **게임 배선을 바꾸면 봇 하네스가 조용히 죽는다**(이 저장소의 반복 패턴). 이번엔 **완주까지
     해버려서 더 조용했다** — 초록으로 끝났고 요약의 "군주 미격파" 한 줄이 유일한 신호였다.
     감시 필터에 그 줄을 안 넣었으면 못 봤다.
- ⚠️ **그 수정은 아직 미검증이다** — 이후 두 판이 4뱃지에 못 가서 고친 코드가 실행되지 않았다.
- 📊 **판별 편차가 극심하다**: 챔피언 31분 / 3뱃지 60분 / 3뱃지 40분(TRACE). 같은 코드다.
  체육관1 도달도 69초~347초로 5배 갈린다. **한 판으로 판단 금지**가 여전히 유효하다.
- 🔍 **전멸(추정) 2463 소동** — 한 판에서 이 값이 나왔다(평소 30~60). 그 카운터는 폴링(700ms)마다
  `alive===0`이면 +1이라 "파티가 전멸 상태로 ~30분"을 뜻한다. 조사 결과:
  게임 무혐의(`faint_modal`·`blackout`·`center` 통과) · 봇은 `#battleMsg`를 안 읽으므로 조사 처리
  변경과도 무관 · 직접 재현 실패 · **TRACE 판에서 재현 안 됨(30)** → **한 판의 이상치로 결론.**
- 📈 **TRACE 집계(40분 판)**: `battle:busy` 39.8% · `story/dlg` 14.6% · `battle:main` 12.8% ·
  `field:grind` 12.4% · `battle:move` 9.2%. `field:wipe`는 상위에 없다 → 정체가 아니라 **처리량 문제**.
  시간의 67%가 전투 안이다. ⚠️ `story/dlg`는 액면대로 읽지 말 것(dt가 한 칸 밀려 앞 분기를 뒤집어쓴다).
- ✅ **게임 쪽 경로는 확정** — `lord_route_test.js` 신규(`1ca2c12`)가 실입력으로 트래커→정면 문→
  가드→제단 A→보스 시작→리그 전환까지 통과. 부하 높은 환경에서도 몇 분이면 돈다.
  ⚠️ 이걸 짜면서 픽스처 함정 4건을 밟았다(전부 게임은 멀쩡): 이동 보간 중 입력 삼킴 · 워프 입력 잠금 ·
     실내/실외에서 재야 할 것을 반대로 잼 · **퇴치 스프레이가 안 먹는 파티**(고레벨만 넣어 평균=선두).

### 2026-08-04 — 서사 보강 (유저: "입구 출구도 없이 갑자기 들어가진다")
유저가 직접 플레이한 뒤 낸 제보. 확인해 보니 **한 문장이 세 가지를 동시에 가리키고 있었다**
(도입부 · 지역/건물 입구 · 중간 서사) → 유저 확인 결과 셋 다. 3단계로 나눠 진행 중.

- **1단계 ✅ 지역 관문 + 장소 표찰**(`d1c0a48`) — 경계 6곳에 기둥+금줄+등(순수 렌더, walkable 불변).
  지역·특수 지역 11곳·루트 7개가 전부 `showPlace()` 한 함수를 지난다. 회귀 `gate_test`(6군).
  ⚠️ 안내가 `#mapHint`에 실려 있었는데 그건 **가로에서 display:none**이다 — 가로 플레이엔
     지역 안내가 아예 없었다. 표찰은 양쪽에서 뜬다.
  ⚠️ 처음엔 꽉 찬 들보였는데 스크린샷에서 **울타리로 읽혔다**("왜 통과되지?") → 늘어진 금줄로.
- **2단계 ✅ 집 아침 시퀀스**(`056a1c0`) — 눈뜨기(암전→깜빡임) → 첫 동료가 깨움 → 머리맡 편지 →
  엄마의 배웅. 조작 튜토리얼은 집을 나선 뒤로 미뤘다(서사와 같은 자리에서 싸운다).
  회귀 `opening_test` — 상태 조립이 아니라 **타이틀부터 실입력으로** 판다.
  ⚠️ 편지 렌더를 실외 타일 체인에 넣었더니 **타일은 P인데 화면엔 아무것도 없었다** — 실내는
     별도 분기가 전부 처리하고 모르는 타일을 바닥으로 떨어뜨린 뒤 return한다. 실내 분기 안으로.
  ⚠️ `playthrough_test`가 실패했지만 **게임이 아니라 픽스처**였다 — `clearDialog`가 "지금 치울 게
     없다"를 "끝났다"로 읽어 눈뜨기 연출(2.1초) 도중 방향키를 눌렀다. G.busy 상태를 보게 고쳤다.
- **3단계 ✅ 중간 서사**(`9d5ba1d`·`801f20b`) — `design-strategist`가 낸 전략(`outputs/strategy/`)의 핵심은
  "컷신이 적다"가 아니라 **라우팅 문제**였다: 이 게임엔 완성된 결말이 둘 있는데(플롯: 군주→EPILOGUE→
  showEnding · 메타: 챔피언→LEAGUE_WIN) `currentGoal()` 사슬에 **군주가 아예 없어** 트래커를 따라가면
  진짜 결말에 도달하지 못했다. 편지도·4뱃지 안내도·라이벌 4차전 대사도 전부 제단을 가리키는데 트래커만
  리그로 보냈다. → 유저 승인(원안)으로 `4뱃지 → (전설) → 숲의 군주 → 리그`로 재배선.
  `creative-producer`가 Bit 1·3·2 구현(라이벌 동기 · 호수 전설전 대사 · 깊은 숲 컷신).
  📌 실측: **라이벌 4연전은 회피 가능하다**(시야 감지 없음, NPC는 1칸만 막음, 4명을 벽으로 놔도 760칸 연결).
- **전체 감사 ✅ 17건 전부 처리**(`6ef89a4`·`f5ac2e0`) — 유저 "게임 전체적으로 볼 수 있게 해야지".
  읽기 전용 감사 에이전트가 실제 플레이 + 스크린샷 40여 장 + DOM 실좌표로 훑었다. 상세는 그날 커밋 메시지와
  `outputs/research/2026-08-04_spirit-grove_game-audit.md`.
  큰 것: 기술머신 13종의 **영문 타입 키 노출**(이미 한 번 고친 클래스의 재발) · 가로에서 **flashHint 83곳이
  통째로 침묵**(#mapHint가 가로에서 display:none인데 그게 유일한 출구였다) · 한국어 어절 중간 줄바꿈 ·
  괄호형 조사 145건 · 상단바가 모든 세로 폭에서 2줄 · 전투 HUD에 **내 정령 타입칩 없음**.
  ⚠️ 조사 145건은 **각 호출부를 고치지 않았다** — 출력 길목 5곳에서 받침으로 고르는 `fixJosa` 하나로.
  ⚠️ `.msg`의 gap을 0으로 고쳤더니 **띄어쓰기가 통째로 사라졌다**("야생설을삐미정령이 나타났다!") —
     flex가 공백 텍스트 노드를 렌더하지 않아 그 gap이 띄어쓰기까지 겸하고 있었다. **스크린샷이 잡았다.**
  ⚠️ CSS **특이도 함정을 두 번** 밟았다 — `.topbar .pill`이 좁은 폭 전용 `.pill`을 덮어써 축소가 조용히 무시됐다.
  ⚠️ 새 테스트의 단정이 처음엔 **공허하게 통과**했다(노출 안 된 함수를 `&&`로 호출 → 건너뜀) → 존재 자체를 단정으로.
  회귀 3종 신규: `gate_test` · `opening_test` · `text_quality_test`. topbar 단정을 "안 잘림"→"세로에서도 1줄"로 상향.

### 2026-07-29 (3) — 게임 디자인 평가 → 후속 7건 + 지선 3개 (콘텐츠·밸런스 10커밋)
*(이 블록은 2026-08-04 세션에서 소급 기록했다 — 당시 커밋만 남고 WORKLOG가 (2)→(4)로 건너뛰어 있었다.
근거는 각 커밋 메시지에 온전히 있고, 여기 적은 건 그 서사와 판단 경계다.)*

게임을 **데이터로 평가**해 갭 7개를 찾고 순서대로 닫았다. "재미없다"는 감상이 아니라
로스터 타입 분포·타입별 기술 수·종당 학습 기술 중앙값·종족값 폭 같은 **셀 수 있는 값**에서 출발했다.

- **지선 루트 3개 추가**(`8343bbd`·`a310dcc`) → 4개 → **7개**. 좌표는 `route_probe.js`로 실제
  `walkable()` BFS해서 **도보 거리가 먼 쌍**을 골랐다(골짜기 21칸·벼랑길 25칸·오솔길 25칸을 건너뛴다).
  ⚠️ 이끼 골짜기의 **모닥불(R)이 안 그려졌다** — 야외 스타일 목록에 안 넣어 동굴색 폴백으로 떨어졌고
  그 분기가 끝에서 return했다. 주석이 이미 경고 중이었다. → 렌더 4곳 + R 케이스 추가.
- **디자인 멀티에이전트 환경**(`a10f3f9`) — 이 저장소에 BX/그래픽/UX·UI/영상 작업용 서브에이전트
  3종과 스킬 4종을 깔았다. ⚠️ **기존 게임 지침 789줄은 삭제하지 않고** `docs/SPIRIT_GROVE_GUIDE.md`로
  옮겨 보존했다 — 게임 작업은 지금도 그 문서가 정본이다.
- **평가 후속 7건**(요약은 가이드 「게임 디자인 평가 후속 7건」, 여기선 판단 경계만):
  1. `aa43c50` 전설 준비 단계 — 완주율이 전설 보유로 갈리는데(Lv48 7.6%→18.4%) 트래커가 4뱃지에서
     곧장 리그로 보내 **설계 의도가 전달되지 않았다**. 안내만 추가 → 밸런스 불변.
  2. `0e2d88d` 땅·독 로스터 복구 — 1차 타입 ground 1종·poison 0종. 땅은 상성표 유리 4개로 가장 강한
     공격 타입인데 **쓸 정령이 없었다 = 전략 공간 20%가 죽어 있었다**. 재타이핑 9종, 아트 추가 0장.
  3. `b90079f` 타입별 공격기 ≥6 — 불 14종이 나눠 쓸 불 기술이 4개였다. 13종 추가.
  4. `6e94562` 학습셋 중앙값 4→8(387→780) — 코드젠 `expand_learnsets.py`로 재현 가능하게.
  5. `9d60326` tier3 종족값 압축 141~224 → 175~205 — 9종 중 **7종 상향·2종 하향**(바닥을 올리는 쪽).
  6. `a310dcc` 초반 밀도 — **이탈이 가장 많은 구간이 가장 비어 있었다**(초반 2지역에 체육관·센터·상점뿐).
  7. `f086815` 진화 라인 8개 — 55종이 이미 최종형이라 셋 중 둘은 성장 연출을 못 봤다.
- ⚠️ **권고안을 두 번 일부러 안 따랐다. 되돌리기 전에 이유를 볼 것.**
  · tier3 상한을 175~200이 아니라 **205**로 — 224/210/205 세 종이 미니보스(천공룡·해일군주·거암왕)라
    과하게 깎으면 **보스 설계가 무너진다**.
  · 진화 라인을 10~15개가 아니라 **8개에서 멈춤** — 「아트/설계 규칙」이 "같은 색·모티프"를 요구해
    억지 연결은 규칙 위반이다. **더 늘리려면 아트가 먼저다.**
- ⚠️ **밸런스 경계선을 이번에도 지켰다** — 신규 기술 13종은 **전부 타입 전용 TM**이고, 학습셋 확장은
  **자연 학습 풀 71종만** 후보로 썼다(TM 전용 37종은 제외). "플레이어 전용 = 트레이너·야생 불변"이
  이 프로젝트의 오래된 원칙이고, 확장 후에도 자연 풀 71 그대로임을 확인해 경계 보존을 증명했다.
- ⚠️ **2·4·5·7은 강함을 바꾸므로 단계마다 재측정했다**(몰아서 재면 원인을 못 가린다).
  그렇게 해서 **#5가 리그 에이스를 강화해버린 걸 잡았다** — 압축이 제피르 회리매를 141→175(+24%)로
  올려 리그가 전 구간 6~10%p 어려워졌다. → `ebc4890`에서 **해당 에이스 레벨만** 낮춰 상쇄
  (팀 구성·마릿수 불변). **자기가 방금 넣은 개선이 다른 지표를 깬 걸 자기가 찾아 되돌린 사례.**
- **테스트 안정화 2건** — 완주율 단정이 몬테카를로 **단일 최댓값에 하드 임계값**이라 같은 코드가
  40.7%/51.3%를 오갔다(가이드가 경고한 안티패턴) → 상위 3지점 평균(σ≈1.5%p) + 임계값을 분포
  한가운데가 아닌 아래(35)로. `ff8daf3`은 인테리어 스왑 대기를 **고정 예산 260ms → 상태 폴링**으로
  (부하가 붙자 거짓 실패했다). **게임 타이밍에 의존하는 검사는 예산이 아니라 상태를 본다.**

### 2026-07-29 (4) — 저장소가 한 번도 packing된 적이 없었다 (457MB → 193MB)
유저: "깃허브 잘 업데이트해놨지?" → 확인해 보니 **원격 `main`은 배포 스냅샷 1커밋뿐**이고
커밋 이력(199개)이 이 맥에만 있었다. 이번 세션 커밋 메시지에 판단 근거가 많이 들어 있어 위험했다.
- **원인은 용량이 아니라 packing 부재였다**: `git count-objects -vH`가 `size-pack: 0` · 느슨한 객체 1885개.
  dist 144버전은 서로 거의 같은 파일이라 델타 압축이 잘 먹는다 → `git gc --aggressive --prune=now`
  한 번으로 **457MB → 193MB**(팩 180MB), 커밋 199개 그대로.
- **이력 재작성 없이 해결**: `git push origin main:history`로 원격에 `history` 브랜치를 만들었다.
  `main`(배포 스냅샷)은 안 건드리므로 Pages·deploy.sh 흐름 불변. 원격 history 199커밋 확인.
- ⚠️ **낡은 결론이 문서에 박혀 있었다** — deploy.sh와 가이드가 "479MB라 이력 push는 비현실적"이라
  적어놔서 다음 세션이 그대로 읽으면 같은 결론을 반복한다 → 둘 다 고쳤다.
  **용량 문제로 뭔가를 포기하기 전에 packing 여부부터 볼 것.**


### 2026-07-29 (2) — 가로에서 못 닫는 패널 (`c8b4c0c`·`92a2c8c`)
유저 제보: "가로 모드에서 지도 들어갔을 때, 닫을 수가 없어 ㅠㅠ / 이런 것들도 신경써서 다 검토 및 수정해줘."
- **제보 1건 → 실제 2건**. `#minimap`이 `.overlay`가 아닌 별도 패널이라 가로 최적화·Escape 배선에서
  통째로 빠져 **완전히 갇혔고**, 전수 감사로 **카드 오버레이**도 하단 닫기가 화면 밖임을 찾았다.
  뿌리는 `.sheet`가 **CSS 정의가 아예 없는 빈 클래스**였던 것(5개 오버레이 공용).
- **고친 방식**: 지도를 예외 취급하지 않고 **예외 자체를 없앴다**(진짜 `.overlay`로 승격 →
  가로 안전영역·스크롤·Escape가 자동으로 따라온다). `.sheet`는 스크롤 컨테이너로 못박아 5곳 동시 해결.
- 회귀 `landscape_panel_test.js` — **패널을 손으로 나열해 눈으로 보는 대신** 열리는 패널마다 같은 두 단정
  (①닫기 컨트롤이 스테이지 실좌표 안 ②Escape로도 닫힘)을 가로·세로 양쪽에서 건다. **13개 패널** 커버.
- ⚠️ **테스트가 실패했는데 게임이 옳았던 경우 2건** — `openSwitch`는 전투 밖이면 안 열리고(전멸 모달 관문),
  전투 패널은 `G.busy`면 안 열린다(정상 가드). **픽스처를 고쳤지 게임을 고치지 않았다.** 주석에 이유를 남겼다.
- 전체 `verify.sh` 실패 0.

### 2026-07-29 — 👑 fullrun 챔피언 완주 (`9b74ecc`·`2d767d4`·`a2a7710`)
WORKLOG가 남긴 미완("60분 판이 뱃지 0, 원인 미특정")을 이어받아 **끝까지 갔다**. 상세는 「현재 상태」 참조.
- **감으로 고치지 않고 `TRACE=1`부터 켰다** — 1분 만에 정지 지점이 나왔다(`OV[bagOverlay] "어떤 볼을 던질까?"` 무한 반복).
- 고친 뒤 **시간 분포를 다시 재서**(트레이스 집계) 벽시계의 70%가 메시지 대기임을 발견 → 빨리감기 홀드로 3배 처리량.
  **"고쳤다"에서 멈추지 않고 한 번 더 잰 게 완주의 분기점이었다** — 결함 16만 고쳤으면 여전히 못 갔다.
- ⚠️ **트레이스 집계는 한 칸 밀려 있다** — `tr()`의 dt는 "이전 분기가 끝난 뒤 지금까지"라, 어떤 분기의 dt는
  실은 **직전 분기의 작업 시간**이다. 같은 분기가 연속되는 구간(`battle:busy`)은 자기보정되지만,
  드문 분기(`story/dlg` 8초)는 앞 분기(`field:grind`의 8초 걷기)를 뒤집어쓴 것이다. 해석에 주의.
- ⚠️ **내가 죽인 걸 브라우저가 죽은 걸로 오독할 뻔했다** — 10분 예산 판을 10분 타임아웃으로 감싸 마지막에
  `pkill`이 들어갔고, 리포트엔 "브라우저가 환경 압박으로 종료됨 · 파티 0마리"가 찍혔다. **바깥 타임아웃은
  예산보다 넉넉히 줄 것.** 실제로는 gym1 안에서 잘 싸우고 있었다.
- ⚠️ **미검증 개선을 넣었다가 되돌렸다** — 4뱃지 뒤 그라인딩 게이트(`GYM_LV` 5칸). 완주는 그것 **없이** 나왔고,
  리그 재도전 루프 자체가 그라인딩이다. 검증된 동작을 미검증 변경으로 덮지 않는다.

### 2026-07-28 (3) — 지선 루트 3개 추가 + 저장소 정리
- **지선 루트 4개 완성**(`eb3def8`·`d29bfca`): 바람 언덕길(초원↔숲, 야외 대각선) · 호숫가 잔교(물 위 판자+낚시) ·
  서리 고갯길(고원↔리그앞, **얼음 미끄러짐** — 체육관3 퍼즐 기믹 재사용). 갱도까지 합쳐 기믹이 서로 다르다.
  맵은 손으로 짜지 않고 **생성 후 BFS로 연결성 검증**(잔교 25칸·고갯길 26칸).
  - ⚠️ 밟은 함정: **시작 칸을 출구 칸과 같게** 잡아 들어가자마자 출구를 밟고 튕겨 나왔다(2곳 다) →
    `route_test [1-b]`가 "시작 칸이 출구와 겹치는 입구 0곳"을 단정한다.
- **저장소 정리**(`83f2ba5`·`d0e01c5`): 아트 원본·오생성 폴더·중복 문서 추적 해제, README에 "다른 기기에서 이어받기".

### 2026-07-28 (2) — 유저 제보 기반 "포켓몬화" 대량 폴리시
유저가 실제로 플레이하며 짚은 것들을 순서대로 처리했다. **감으로 고르지 않고 매번 화면을 찍거나 전수 감사로 확인**했다.

| 커밋 | 무엇 | 발견 경로 |
|---|---|---|
| `05ff8c5` | 아이템 **대상 정령 선택**(전엔 누르면 선두에게 즉시) · **라이벌 레벨/마릿수 스케일링** | 유저 제보 |
| `da923b6` | **정면 출입구만**(4방 부딪힘 제거) · 상점·제단의 **밟기 진입 제거** · 건물 위 NPC 5명 정리 · 막힌 문 앞 5곳 개방 | 유저 제보 + 좌표 전수 조사 |
| `f568f0b` | **전투 전체 화면**(상단바가 15%를 먹고 있었다) · wildFloor 실내 좌표 버그 | 스크린샷 검수 |
| `31a7223` | **비전기술 장면** — 정령이 나와서 자르기/괴력/파도타기를 쓴다 | 유저 제보 |
| `76ed43a` | **맵 오토타일** — 풀숲 덩어리·물/길 모서리·지역 경계 색 블렌드·나무 흔들기 | 유저 제보 + 오버월드 촬영 |
| `8144cca` | 전투 하단 **본가 배치**(메시지 크림박스 좌 · 메뉴 우 2×2) | 유저 제보 |
| `00ae4b4` | NPC 스프라이트 재질(팔·둥근 눈·뒤통수·신발·측면 실루엣) | 유저 제보 + 확대 검수 |
| `52c0571` | **리포트(저장 슬롯 3칸)** · 실내 벽/바닥 입체화 | 유저 제보 |
| `4d34f52` | **실내 인물 전수 통일** — 간호사·회관 3인·관장·가드·엘리트4가 각자 그리고 있었다 | 유저 지적("제발 다 체크") |
| `0c6162c` | 이모지로 서 있던 트레이너 24명 → **역할 테마 스프라이트** · **트레이너 전투 인트로** | 유저 지적("나뭇잎으로 되어있거나") |
| `9d3472c` | **공중날기** — 쉬어본 곳으로 날아간다(모닥불 귀환을 승격, markHeal 단일 관문에서 자동 등록) | 유저 제안 |
| `7c42482` | 전투 UI 4건(**볼 선택**·연속탭 가드·**돌아가기 버튼**·포커스 잔상) + **지선 루트 버려진 갱도** | 유저 제보 |

- **지선 루트가 이 세션의 구조적 성과**: 오버월드가 25×50 세로 복도 하나라 일차원적이었다.
  `exitInterior(alt)`로 **다중 출구**를 지원해 **입구 둘인 통과형 루트**를 놓았다. 현재 **4개**:
  갱도(숲↔깊은 숲, 지하) · 언덕길(초원↔숲, 야외 대각선) · 잔교(수정 호수 내, 물 위 판자+낚시) · 고갯길(고원↔리그앞, 얼음 미끄러짐).
  두 입구 다 원래 도달 가능했던 곳 → **게이팅 불변, 지름길만** 생긴다. 타일 문자는 `$` 하나를 **좌표 스코프**(ROUTE_AT).
  ⚠️ 엔진은 원래부터 임의 크기 맵을 지원한다(인테리어마다 W/H) → **루트를 더 파는 건 콘텐츠 작업**이고 위험이 낮다.
- **다음 지선 루트를 만들 때**: ① 입구 후보를 실제 `walkable` BFS로 뽑고(도달 가능한 `#` 타일) ② `ROUTE_AT`에 두 입구 + 각각의 시작점
  ③ `exits`에 반대편 출구 좌표 ④ reachability의 `all`·`note` **두 곳** 배선 ⑤ 조우 풀은 실존 종인지 `dex.js`로 확인.

### 2026-07-28 — 저부하 완주 재개 → (게임 버그) 닫을 수 없는 전멸 모달
재부팅 직후(load 2.9/8코어) WORKLOG가 기다리던 조건이 맞아 fullrun 완주 검증을 이어갔다. **45분 판이 전투 1회·제자리로 끝났다** — 진행 곡선 샘플이 0건이라(루프 바닥에 한 번도 못 감) 조기 `continue` 무한 반복임을 알았고, `TRACE=1`로 정지 지점을 특정했다.
- **정지 지점**: 필드에서 `OV[switchOverlay] btns=["✕","내보내기[x]"]` 무한 반복.
- **원인(게임)**: `faintMine`은 `await mw(...)`를 여러 번 지난다. 전멸 처리(healParty→endBattle→blackout)가 끝난 뒤 **먼저 시작돼 있던 다른 faintMine 체인이 재개**하면 `alive`를 다시 계산하는데, blackout이 파티를 회복해 놨으므로 `alive.length>0`으로 오판하고 **필드에서 `openSwitch(true)`** 를 연다. 강제 교체는 ✕를 `display:none`으로 감추고 `switchOverlay`는 `CANCELABLE_OVERLAYS`에 없어 **Esc·x·B 전부 무효** → 플레이어도 리로드 말곤 못 벗어난다.
- **수정**: ① `faintMine`이 await 뒤 `!G.inBattle`이면 즉시 return · ② `openSwitch`가 전투 밖이면 아예 안 연다(구조적 관문).
- **회귀** `faint_modal_test.js`: 수정 전 빌드에서 3단정 실패 → 수정 후 통과. **[3] 대조군**(전투 중 정상 강제 교체는 열려야 함)이 양쪽 다 통과해 과잉 수정이 아님을 고정. verify.sh 등록.
- **봇 결함 15건째**: Escape만 믿었다(필드 Escape는 CANCELABLE 목록만 닫는다) → 보이는 `.closex` 우선. + 같은 오버레이가 25루프 버티면 **게임 결함으로 기록하고 강제로 걷어 진행**(조용히 갇히면 판이 통째로 헛돈다).
- 수정 후 5분 판: 전투 16회 · Lv5→10 · 그라인딩 29회 · gym1 진입 · 정지 0. 영향 테스트 7종(blackout·defeat_music·switchmoves·tower·battle_bag·foe_switch·duo) GREEN.
- ⚠️ **교훈**: "런타임 에러 0"은 멀쩡하다는 뜻이 아니다. 이 버그는 에러를 한 줄도 안 내면서 게임을 못 쓰게 만든다. **진행이 멈췄다는 사실 자체**가 신호였고, 트레이스가 5분 만에 지점을 찍었다.

### 2026-07-23 — 전수 플레이스루: 뱃지 2/4 도달, 진행 곡선 건강
메모리 회복(≈1.1GB) 후 20분 실측. 봇이 **두 게이트를 통과**하고 깊은 숲까지 올라갔다:
- 🏁 체육관1 305초(파티2·Lv10·전투7) · 🏁 체육관2 770초(파티5·Lv19·전투21) · 최종 뱃지 2/4, 파티 5마리 Lv[13,14,19,17,17], 깊은 숲(9,30).
- **곡선이 건강하다**: 파티 1→5마리 자연 증가, 레벨 5→19 정체 없이 상승, 뱃지 간격 305→770초(뒤 게이트가 더 어렵다는 설계와 부합). ⑪ 수정(뱃지 후 탈출)이 정확히 먹혔다 — 지난 판 22분 갇힘 → 이번 12분에 뱃지2+깊은 숲.
- **게임 버그 여전히 0건** — 20분·전투 22회·회복약 16회 자동·센터/문/견습생 실입력 처리에서 런타임 에러 0.
- 남은 것: 20분으론 체육관3 못 감. 게이트당 ~7~8분이라 챔피언 완주엔 **40~50분** 필요. 다음: 긴 판으로 후반 게이트(특히 회복 없는 리그 5연전)에서 봇이 버티는지 + 밸런스 문서 도전 레벨 vs 실제 봇 레벨 대조.
- ⚠️ **후속(같은 날)**: 45분 판을 두 번 시도했으나 **둘 다 브라우저가 5분 내 사망/OOM 킬**됐다(이 머신은 1일+ 가동, load ~6.8이라 장시간 헤드리스를 못 버틴다). 크래시 관용이 진행을 보존해 손실은 없었다. **결론: 이 환경에선 챔피언 완주 실측 불가.** 부하 낮은 환경(재부팅 직후·load<3)에서 재시도할 것.
  - 다만 **후반 게이트의 밸런스는 이미 충분히 측정돼 있다** — `balance_test`(체육관3·4·군주·챔피언 각 300판)·`league_test`(리그 5연전 250판)가 몬테카를로로 커버한다. `fullrun`의 고유 가치는 "배선이 끝까지 이어지는가"인데 그건 **두 체육관에서 실증됐다**(문 진입·센터 회복·견습생·기절 교체 전부 실입력 처리, 게임 버그 0). 즉 남은 건 *측정된 미지*가 아니라 *실연되지 않은 배선*일 뿐이고, 그 위험은 낮다.


### 2026-07-23 — 전수 플레이스루: 첫 뱃지 돌파 (봇 결함 11건째 수정)
지난 세션 이어서. 트레이스로 정지 원인을 계속 특정해 **봇이 처음으로 게이트를 통과했다**(뱃지 1/4, 3분 22초).
- **⑨ 문에 안 부딪힘**: 목표(체육관·센터·리그)는 비보행 문인데 봇이 문 앞 칸에서 목표만 재탭하며 맴돌았다(walkTo가 0=이미 도착 반환). → 목표가 바로 옆이면 부딪혀 진입.
- **⑩ 체육관에서 도로 나감**: 실내 탈출 로직이 체육관에도 적용 → 체육관·리그는 목표이므로 위로 걸어 관장에게 부딪힌다.
- **⑪ 뱃지 후 갇힘**: 격파한 관장 앞에서 계속 위로 걸었다(25분 중 22분 낭비). → `st.badgeKeys.includes(N)`이면 나간다.
- **되돌린 시도**: `currentGoal` 좌표를 `walkTo`로 끝까지 — 더 나빠져 되돌렸다. 진짜 문제는 경로가 아니라 ⑨였다.
- **브라우저 크래시 관용 추가**: 환경 압박으로 헤드리스가 죽는 건 게임 버그가 아니므로, 그때까지 진행을 스냅샷 보고하고 정상 종료.
- **게임 버그는 여전히 0건**(누적 11건 전부 봇 쪽). 게임은 25분 연속 플레이·매 강제상황에서 정상 동작.
- ⚠️ **이 환경은 지금 메모리 ~60MB만 남아 장시간 런을 못 버틴다**(브라우저 즉사). 코드는 정상(90초 런 OK). 20분+ 실측은 **부하 낮은 환경에서 재시도** — 그때 체육관 2~4·리그가 실제로 걸린다.


### 2026-07-22 — 전수 플레이스루 하네스 착수 (미완, 인계)
"포켓몬과 똑같아졌나"에 답하려면 **끝까지 한 번 걸어봐야** 한다는 판단. `longrun_test`가 첫 뱃지에서 멈추기 때문에 그 뒤 구간(체육관 2~4·숲의 군주·리그·챔피언)은 **아무도 실제로 플레이해본 적이 없다.**
- `scripts/fullrun_test.js` 신규(`557ef35`). 목표 트래커 이동 + 상성 기술 + 포획 + 센터 회복 + 오버레이 처리.
- **지금까지 나온 건 전부 봇 결함 6건, 게임 버그는 0건.** 게임은 10분 연속 플레이에서 런타임 에러 0으로 버텼다(도주 정책·간호사 다단 대사·강제 교체 오버레이·실내 탈출·회복약 성공 판정 등 — 상세는 파일 상단 주석).
- ⚠️ **아직 첫 뱃지도 못 간다.** 1~5분 사이에 루프가 멈추는데 원인 미규명(`.overlay.active` 처리 분기가 유력).
  디버깅은 **매 루프 상태를 파일로 덤프해 정지 직전 3~4스텝을 보는 것**이 가장 빠르다.
- ⚠️ `verify.sh`·`playtest.sh`에 **일부러 등록하지 않았다** — 신뢰할 수 없는 상태로 CI에 넣으면 노이즈만 된다. 완성되면 등록할 것.
- 참고로 관측된 초반 난이도: 스타터 Lv5로 나가면 지역1 야생이 `WILD_FLOOR=9` 때문에 **Lv8~16**으로 나온다. 문서화된 의도("덜 싸운 플레이어를 끌어올린다")지만, 초반 체감이 실제로 어떤지는 이 하네스가 완성돼야 답할 수 있다.


### 2026-07-22 — 죽은 콘텐츠 사냥 + 화면 검수 폴리시 (한 세션에 실버그 6건)
"코드는 있는데 플레이에서 닿지 않는다"를 각도별로 훑고, 주요 화면을 스크린샷으로 검수했다. **감으로 폴리시 항목을 고르지 않고 증거에서 출발한 게 주효했다.**
- **learn 배열 중첩 손상**(`7bf246f`): `[16,"bite",[28,"nastyplot",[36,"roar"]]]` — 형제 항목이 중첩돼 **8종이 첫 기술만 배우고 있었다**(13기술 소실). 발견 경로는 "아무도 못 배우는 기술" 감사. 남은 6종은 범용 TM으로 개방. **학습셋 변경 = 트레이너/야생도 바뀜 → 밸런스 재측정**(노이즈 범위 확인).
- **아이템 이름표가 키 노출**(`26ea432`): "ether을(를) 주웠다!" — `ITEM_KO`에 TM·기력·엘릭서·볼·지닌물건이 없었다. `itemName(k)` 단일 조회로 통일(호출부 7곳). `tm_confuse`는 아예 못 얻는 아이템이라 상점에 추가.
- **전투 화면 좌상단 삼중 겹침**(`4c23914`): 등장 라벨·날씨 칩이 적 HUD를 덮고, 타입칩이 63px 벌어져 있었다.
- **정령 관리 카드**(`bdb596a`): HP 위험색·상태칩 부재 + 카드 204px.
- 새 회귀 4종: `dead_content_test`(획득 경로·이름표·트레이너 배치) · `battle_layout_test`(좌표 겹침) · `pc_card_test` · rules_unit_test에 학습셋 위생 5단정.
- ⚠️ **레이아웃/DOM 테스트의 공통 함정**: 뷰(오버레이·#battle)를 실제로 열지 않으면 **모든 rect가 0**이라 "겹침 없음"·"높이 정상"이 거짓 통과한다. 두 번 밟았다 → 두 테스트 모두 **"실제로 그려졌는가" 단정을 같이** 둔다.
- ⚠️ 부하가 높으면(`load>10`) `movefx_variety`·`twoturn`·`door_bounce`가 간헐 실패한다. **격리 재실행으로 판별할 것** — 이번에 한 번은 "내 회귀"라고 잘못 판단했다가 재실행으로 정정했다.


### 2026-07-22 — (대형) 특수 지역 11곳의 야생 조우가 죽어 있었다
게임필 폴리시 방향을 감으로 정하지 않고 **주요 화면을 스크린샷으로 찍어** 갭을 찾으려다 발견.
첫 전투 스크린샷에 **Lv5 스타터 앞에 Lv33 야생**이 찍혀 있었고, 그 실마리에서 버그 2건이 나왔다:
- **조우 굴림 미도달**: `onArrived`의 실내 블록이 `continueMovement(); return;`으로 끝나는데 조우 굴림은 그 뒤 실외 구간에 있었다 → 동굴·용암굴·늪지·설원·해안·섬·신전·봉우리·유적·분화구·화원 **11곳 조우가 전부 죽은 코드**(늪지 20걸음 0회). 이번 세션에 만든 분화구·화원도 포함이었다.
- **실내에서 오버월드 조우**: 체육관2엔 `T`, 리그엔 `g`가 있어 굴림을 열면 `startEncounter`가 돌고 `wildFloor()`가 인테리어 좌표를 지역으로 읽어 Lv33 야생이 나온다 → `!G.indoor`로 차단.
- 수정: `wildRoll(t,depth)` 실내/실외 공용 단일 출처. 회귀 `indoor_encounter_test.js`는 **함수를 부르지 않고 키 입력으로 걸어서** 센다(수정 전 빌드 실패 확인).
- ⚠️ **교훈**: 기존 회귀들이 `start*Encounter`를 직접 호출해 통과시키고 있었다. "함수가 있다"와 "플레이에서 걸린다"는 다르다 — **새 콘텐츠는 반드시 실제 입력으로 한 번 걸어볼 것.**
- 검증: verify 2구간 GREEN · playtest 7종 통과(밸런스/리그 설계 단정 유지 — 조우는 시뮬 모델 밖이라 수치 불변).


### 2026-07-22 — 규칙 계층 확장 + 풀숲 게임필 (두 갈래 동시)
- **테스트 이관**(`e53c6c8`): movedesc·legendary가 브라우저를 띄우던 이유는 검사 대상 때문이 아니라 `MOVES`/`DEX`가 HTML에 갇혀 있어서였다 → `src/rules/moves.js`·`dex.js`로 내리고 두 테스트 파일 삭제, 같은 커버리지를 `rules_unit_test`로(+ makeMon 위생 단정). 로드 순서 util→tables→moves→dex→battle.
  - **데이터를 규칙 계층으로 옮기는 건 병렬 테이블 만들기가 아니다** — 정의가 옮겨갈 뿐 사본이 안 생긴다(build.py가 같은 파일을 인라인). 이 구분을 CLAUDE.md에 명시해뒀다.
  - ⚠️ 추출 중 `noPP` 함수 머리를 같이 잘라먹어 dist가 문법 오류가 났다. **잘라낸 뒤 반드시 `node --check`**로 확인할 것(verify의 문법 게이트가 잡아줬다).
- **풀숲 게임필**(`8166cb9`): 술렁임·하체 가림·달리기 먼지. 전부 `_grassFx` 한 곳 + reduceMotion 일괄 차단.
  - 회귀는 픽셀 비교 대신 **효과별 고유 색을 태그 삼아 드로우 콜을 센다** — 캔버스 연출을 안정적으로 검증하는 방법으로 재사용할 만하다.
- 두 갈래 끝난 뒤 **playtest 7종 재실행 전부 통과**(도달성 96/21 · 몽키·장기·실입력 에러 0 · 밸런스/리그 설계 단정 유지).


### 2026-07-22 — 바다 미니보스(해일군주)로 NO_WILD 대칭 완성
`NO_WILD` 3종 중 천공룡·거암왕만 보스가 있고 해일군주만 획득 경로가 진화뿐이었다 → 여명의 섬 북쪽 곶에 조수 제단 `@`(12,1). 봉우리·유적과 **완전히 같은 배선**이라 새 개념 0.
- 해안(`W`)이 아니라 **섬**에 둔 이유: 해안은 시작 마을 옆이라 초반에 Lv45 보스를 만나게 된다. 섬은 낚싯대+배 게이트가 이미 걸려 있다.
- 검증: 실제 키 입력으로 제단에 걸어 들어가 보스 조우 확인(`tile:"@" walk:false foe:"tidalore"`) · reachability(인테리어 21곳) · skyridge_test 3단정 추가 · verify 구간1 GREEN · legendary·blackout·dex_flavor 재확인.

### 2026-07-22 — 신규 지역 2곳(분화구·화원) + 인테리어 NPC 누수 수정
"지역 더 파기" 선택. 특수 서식지에 없는 32종을 데이터로 뽑아 테마가 뭉친 두 갈래를 골랐다:
- **불꽃 분화구**(`dc37ec2`): 불 14종 중 8종이 서식지가 없었다(용암 동굴 풀은 바위/땅 중심이라 실질적 불 서식지가 아니었음). 진입 `^`(22,12) · 가드 `*` · 조우 10종 **전원 불 타입**(회귀가 강제).
- **달빛 화원**(`f198f1e`): 나방·꽃 계열 + 서식지 없던 풀 타입. 진입 `!`(4,36) · 가드 `?` · **낮/밤으로 조우 풀이 갈리는 유일한 지역**(밤 10종/낮 6종, 조우율 0.17/0.11). 도감 설명문과 조우를 일치시키는 게 목적.
- **기존 버그**(`ab93d35`): NPC 렌더 패스·미니맵 마크에 `G.indoor` 가드가 없어 **모든 실내**에 오버월드 NPC가 유령처럼 서 있었다. `walkable`은 이미 스코프돼 있어 말도 안 걸리는 허깨비라 오래 안 들켰다. 회귀는 `fillText`+`Map2D._char`를 둘 다 가로채 센다(대부분 NPC가 `spr`이라 fillText만 세면 대조군이 0). 부모 빌드 72회 → 0회로 실패/통과 확인.
- 검증: verify 3구간 GREEN · playtest 7종 · reachability(96/21) · 스크린샷 4장.
- 지역 추가 후 **playtest 재실행도 전부 통과** — 도달성 96/21 · 몽키·장기·실입력 에러 0 · 밸런스 도전 레벨 -4/0/-4/-4/-2/0 유지 · 리그 설계 단정(엘리트 관문 39.6%p · 챔피언 벽 54.4%p · 전설 보상 +13.6%p) 유지.
  신규 지역은 **게이트 밸런스에 무관**하다(가드 2명은 선택 콘텐츠, 학습셋·게이트 팀 불변).


### 2026-07-22 — 확장 후 실플레이 검증 (playtest 7종 전부 통과, 버그 0)
패리티 11종 + 신규 지역 3건이 들어간 뒤의 첫 실플레이 검증. `bash scripts/playtest.sh dist/spirit_grove_3d.html 40 90`.
**게임 코드 버그 0건** — 고칠 것이 안 나왔다. 측정값만 기록해 둔다:
- **도달성**: 오버월드 콘텐츠 **94개** 전부 도달(NPC 42명 · 실외 도구 20개 · 센터 7 · 회관 3) + **인테리어 19곳** 내부 전부 도달. 신규 지역 3건(늪지·봉우리·유적)이 도달 목록에 반영돼 있다. 런타임 에러 0.
- **실입력 플레이스루 / 몽키 퍼즈(40s) / 장기 플레이스루(90s)**: 전부 에러 0 · 잠금 0 · 상태오염 0. 몽키 busy 최장 4.6초(정상 범위).
- **밸런스 실측(각 300판)**: 의도된 도전 레벨이 게이트별 -4/0/-4/-4/-2/0 로 유지. 언더레벨(-6) 무쌍 게이트 0.
- **리그 연속 관문(각 250판)**: 완주율 Lv48 7.6%/18.4% · Lv51 22%/39.6% · Lv54 42.4%/61.6%(전설 없음/1마리).
  CLAUDE.md에 적힌 옛 수치(48: 14.5%/25%)보다 **낮게 나오는데 회귀가 아니다** — 시뮬에 상태이상 모델링이
  들어간 뒤(`56422cf`) 값이 내려간 것 + n=250 몬테카를로 노이즈. 설계 단정(엘리트가 관문 · 챔피언이 최종 벽 ·
  전설 준비 보상 +15.9%p)은 전부 유지된다.
- ⚠️ longrun의 **전멸 3회**는 소지금 프록시 추정치(경향치). 90초 예산이라 뱃지 0 — 예산을 늘려야 뱃지 구간이 잡힌다.

### 2026-07-21 — 신규 지역 미니보스 (봉우리·유적 클라이맥스)
"계속 파" 지역 심화의 마무리. 두 신규 지역에 캡스톤 보스를 얹어 **진화전용(NO_WILD) 종의 대체 획득 경로**를 열었다:
- 봉우리 정상(`@` 7,1)=천공룡(skydrake, ~Lv46) · 유적 중앙(`@` 7,6)=거암왕(megalith, ~Lv44). 둘 다 원래 진화로만 얻던 종 → 보스전으로 잡을 수 있는 유일한 길.
- **아트 0장**(기존 종 재활용) · **비전설**이라 정상 포획률 · 렌더는 shrine의 황금 제단 `@` 재활용(`G.indoor` 스코프 분기, 3 인테리어 공용).
- `startSkyBoss`/`startRuinsBoss`(startSnowLegendary 템플릿) · 스코프 타일 핸들러 2건 · done-flag `skyBossDone`/`ruinsBossDone`을 winBattle·tryCatch + freshState/serialize/deserialize에 배선(빙하제 패턴). 재방문 대사.
- 검증: 전체 `verify.sh` 3구간 GREEN(skyridge_test에 보스 조우/done-flag 왕복/NO_WILD 단정 추가) + reachability(인테리어 19곳 @ 자동 커버) + dex_flavor·region_content·blackout·smoke + 스크린샷(유적 황금 제단 렌더 확인).

### 2026-07-21 — 신규 지역 2곳 (봉우리·유적)
"지역 파고 들자" → 서식지 없는 타입군을 위한 신규 지역 2곳. **늪지 템플릿(marsh) 19-포인트 배선을 재사용**:
- **뇌명 봉우리**(`a718fc3`): 전기/비행 서식지(전기-새/뱀 라인 8종이 어느 특수풀에도 없었음). 진입 `O`(17,1) · 가드 `y`(폭풍지기) · 조우 8종 · fieldMusic="highland".
- **고대 유적**(`b50b027`): normal/rock/ground "고대/석 계열" 10종. 진입 `z`(14,27) · 가드 `h`(유적 수호자) · fieldMusic="cave".
- 회귀 `scripts/skyridge_test.js`가 두 지역을 병렬 커버. reachability·dex_flavor·region_content가 자동 반영(HABITAT_KO 단일 출처).

### 2026-07-21 — 포켓몬 패리티 대확장 (조사 기반, 고가치 전부)
서브에이전트로 패리티 갭을 전수 조사한 뒤 값어치순으로 구현. 각 커밋마다 회귀 테스트 신규 + 시뮬 파리티(해당 시) + 영향 테스트 재확인:
- 포획 루프(`5155c87`): 상태이상 배율(잠듦/냉동↑) · 볼 7종 · 크리티컬 캡처.
- 스탯 아이템(`643098a`): 영양제(EV)·민트(성격)·병뚜껑(IV) — 백엔드는 이미 있었음.
- 배틀 기술(`54a2fe4`): 자폭·헤롱헤롱·묶기.
- 낚싯대 등급(`16cb564`): 구/좋은/초 → 대물 확률↑.
- 2턴기(`572c8cc`): 날기(충전+반무적)·솔라빔.
- 기술 제어(`cd438aa`): 앵콜·도발·방해.
- 신규 배틀 기술은 **전부 범용 TM(플레이어 전용)** 로 획득 → 학습셋 불변 → **밸런스 완전 불변**.

### 2026-07-21 — 콘텐츠 팩 + 도구 줍기 + 이벤트 감사
- 도구 줍기 눌러-받기(`b0d71d1`): 자동 흡입 → A로 줍기(발견 대사+팡파르). 이벤트 감사로 `q_bond` 도달불가 버그 수정 + 인테리어 바닥볼 가시성 수정.
- 콘텐츠 팩(`d58f8a7`): HM힌트 NPC·코스트/신전 도구·건틀릿 퀘스트·**열매나무(재생 자원)**.

### 2026-07-21 — "포켓몬처럼" 개선 4종 (조사 → 진짜 빈 곳만)
서브에이전트로 캐논 시스템을 전수 감사(대부분 이미 구현+폴리시돼 있었음 — PC 보관함·포획 흔들림/도감등록/별명·레벨업 스탯표시·VS시커·러닝·정령 요약·EV/IV/성격·브리딩·리펠). **진짜 빈 곳/반쪽 4개**만 골라 하나씩 구현:
1. **지닌 물건 확장**(`a035bec`): 생명의구슬·기합의띠·구애머리띠/스카프 + 타입 부적 10종(TYPES 파생). 전투 효과를 `HELD_ITEMS` 데이터 필드로 단일화 → damage/effSpd·듀오·시뮬 자동 일치. 학습셋에 안 넣어 밸런스 불변.
2. **오버월드 날씨**(`6768b64`): 있던 전투 날씨(RNG)를 `owWeather()`(지역+실시간 시계, 결정적)로 통합 → "필드에서 본 날씨=전투 날씨". 필드 렌더(빗줄기·눈송이·안개·햇살)·조우 편향. 실내 무날씨(부수 개선).
3. **교환소**(`1035e2b`): 리그 앞 교환상인 — 코인→타입 부적 10종(#1과 연결)·진화의 돌·반짝임의 부적(샤이니↑). 후반 3만 코인 소비처.
4. **탈출로프**(이 커밋): 막다른 던전에서 오버월드로 즉시 탈출. `ESCAPABLE` 4곳만, warpFade 지연 스왑 후 저장.
- 각 커밋마다 회귀 테스트 신규(rules_unit 확장 + helditems·weather·exchange·escape) + 스크린샷 시각 확인. 영향받은 기존 테스트 재확인 통과.

### 2026-07-21 — 전투 페이스 완화 (게임필)
"전투가 너무 빠르고 정신없다"는 피드백 → **메시지 대기(`mw`)만** 전투 중 `BATTLE_PACE`(1.35)배 늘렸다. 히트 애니(`fxT`/`wait`)는 안 건드림(쫀득함 유지). 오버월드 mw는 그대로(전투 중만 분기). 설정 "속도" 항목 문구를 "전투 속도"로 명확화(느림→느긋). 단일 노브라 조정 쉬움. 전투 타이밍 테스트 9종 통과 확인.

### 2026-07-21 — 인수인계 잔여 4항목 착수 (방어·테스트예산·이관·트레이드결론)
WORKLOG "남아 있는 것" 4개를 한꺼번에 처리 중. 이 커밋은 앞 3개:
- **playthrough 15초 고정 예산 → 실제 시계 예산**: busy 대기를 45초 실측 + `os.loadavg()` 판별(코어당 load>2면 환경 오탐으로 경고만, 낮으면 하드 실패). 부하 높을 때 게임 버그 아닌데 실패하던 것 해소.
- **브라우저 테스트 감축**: `type_chart_test.js`(상성표)를 `rules_unit_test.js`로 이관(`R.EFF` 직접 단정 = damage().eff와 동치이자 더 순수) → playwright 목록에서 제거. 나머지 후보는 `MOVES`/`DEX` 추출 선행 필요라 보류(문서화).
- **본가 대조 판단보류 결론**: (a) **트레이드 진화 = 갭 아님**(P2P 교환 없음, 유일 계보 megalith는 이미 `evolveLv:36`, NPC 트레이드는 완성형 지급) → 구현 안 함. (b) **방어(protect) 구현**(아래), **대타출동(substitute)은 보류**(가시 인형 UI 없이는 HP바가 얼어 혼란 + 로직 L 규모).
- **방어(protect)**: 위력0·pri4·`eff.protect`. 네 전투 시스템 전부(메인 `performMove`·듀오 `dbExec`·적 AI `foeChooseMove`·시뮬 `battle_sim.js`)에 차단/발동/연속실패(`1/3^streak`)/리셋 일관 구현. 학습셋엔 안 넣어 **밸런스 불변**, 범용 TM `tm_protect`로 획득. 회귀 `scripts/protect_test.js`(load 11에서도 통과). 영향받은 배틀 테스트 6종(stage_rank·switchmoves·foe_switch·duo·movedesc·sim_status) 개별 재확인 통과.
- **Task 4 완료** — `playtest.sh` 하네스 실행 버그 사냥. **게임 코드 버그는 0건**. 대신 테스트 하네스 2건을 견고화:
  - **playthrough**: `busy=true`로 결정적 실패하던 게 **부모 커밋에서도 재현**(내 회귀 아님) → 근본원인은 전투 후 이동 체크(moved2)가 **표지판/NPC 대화로 걸어 들어가** showDialog가 busy를 정당하게 잡은 걸 "stuck"으로 오판한 것. settle 루프(최대 3초)로 전환이 해소되길 기다린 뒤 inBattle/대화/부하로 분류하게 수정.
  - **monkey**: 게임 버그 0건. 진행량 게이트가 `visited>=20||battle>=2`라 부하로 입력 스로틀되면 죽음(load 12에 216입력이 9칸밖에) → CLAUDE.md 원칙대로 **약한 바닥**(`visited>=5||battle>=1`)만 게이트, 수치는 참고 출력.
  - reachability·longrun은 그대로 통과(버그 없음). balance/league(몬테카를로)는 protect가 학습셋에 없어 **구조적 불변**이라 부하 높을 때 생략.
  - 게임필 폴리시: 별도 방향 지정이 없어 미착수(추천 방향 정해지면 이어서).

### 2026-07-21 — 본가 대조 4건 (`d415681`)
- **무효(0배) 전용 문구** + 급소·다단히트가 상성 문구를 묻어버리던 else-if 사슬 제거.
- **닉네임이 전투 서술에 반영** — HUD는 닉네임인데 턴 서술은 종족명이던 불일치.
- **듀오 배틀이 면역 관문을 우회**하고 있었다 → 규칙 계층(`statusBlockReason`)으로 통합.
  문서엔 "applyStatus가 유일한 관문"이라 적혀 있었지만 실제론 아니었다.
- **퇴치 스프레이**가 설명("약한 정령 차단")과 달리 60걸음 완전 무적이었다 → 본가식 레벨 판정.

### 2026-07-21 — 밸런스 하네스 상태이상 모델링 (`56422cf`)
- 오래 남아 있던 사각지대 제거: 하네스가 `eff.status`를 아예 안 봐서, 상태이상 기술 18종을
  전부 무시한 채 난이도를 재고 있었다. 이제 실제로 잰다.
- 부수 발견: 두 하네스의 시뮬 사본이 **이미 드리프트**해 있었다(balance 쪽 반동 = `maxHp*0.06`).
  `scripts/battle_sim.js` 하나로 통합.
- 재측정 결과 게이트 설계는 버틴다 — 이제 "측정된 사실". 리그는 격차가 더 벌어져
  (개별 곱 78.9% vs 실제 완주 17.6%) 회복 없는 연속전 설계가 수치로 더 뚜렷해졌다.

### 2026-07-21 — 순수 규칙 분리 (`522e5a5`) — 아키텍처 개선 2순위
- 테스트 64개 중 56개가 브라우저를 띄우던 원인(코드가 HTML에 갇힘)을 걷어냈다.
  `src/rules/{util,tables,battle}.js` — 옮기기 전에 **DOM 참조 0으로 측정**하고 옮겼다.
- 핵심 설계: 브라우저와 node가 **문자 그대로 같은 코드**를 돈다.
  build.py가 마커에 인라인 / `rules_env.js`가 같은 파일을 같은 순서로 vm에서 평가.
  파일별 `module.exports`는 일부러 안 썼다 — export 목록이 또 하나의 병렬 테이블이 되니까.
- 결과: 46개 단정이 **0.07초**. verify.sh 맨 앞에 배치.

### 2026-07-20 — 타입·상태 단일 출처 (`de05acf`) — 아키텍처 개선 1순위
- 이 프로젝트에서 가장 자주 재발한 버그 종류("병렬 테이블 중 하나 빠뜨림")를 **구조적으로 제거**.
  `TYPES`/`STATUSES` 하나에서 테이블 13종 + CSS + 도감 필터 + 울음 파형까지 전부 파생.
- 하는 김에 드러난 누락 2건도 메워짐: `TYPE_ICON` 7종뿐(얼음·독·땅 아이콘 없음),
  `Audio.cry` 파형도 7종뿐(신규 3타입은 기본값 울음).
- 회귀 테스트는 **키를 나열하지 않고 순회**하도록 짰다 — 나열하면 테스트가 또 하나의 병렬 테이블이 된다.
- 브라우저 테스트 55종 격리 실행 전부 통과(재시도 0), 스크린샷으로 렌더 확인.

### 2026-07-20 — 남은 4건 (`634eb56`)
- 맹독 누적 · 다단히트 가중치 · **로드스컴 차단** · 상점 진행도 게이팅.
- 로드스컴은 버튼만 막아선 안 됐다 — `battleUseItem` 등이 **매 턴 자동 저장**을 불러서
  `saveGame` 자체를 막아야 했다. 조사 단계에서 "저장 버튼"만 봤으면 놓쳤을 부분.
- 맹독은 별도 status로 쪼개지 않고 `psn` + `_tox` 카운터로 갔다 — 본가에서 맹독이
  '독의 변종'이라 해독제·타입면역·특성이 전부 동일 적용돼야 하기 때문.
- 브라우저 테스트 53종 격리 실행 전부 통과, 밸런스·리그 재측정 통과.

### 2026-07-20 — 버그 사냥 5건 (`7415bde`)
- 서브에이전트 3명으로 전투/필드/코드위생을 훑고, 나온 주장을 전부 코드로 직접 재검증한 뒤 5건 수정.
- 제일 값어치 있던 것: **반동 공식**(입힌 피해가 아니라 자기 최대 HP 기준이라 반동기가 쓰면 손해였다)과
  **타입 상태이상 면역 부재**(불이 화상, 독이 중독).
- 부수 수확: CLAUDE.md가 "회귀가 HIDDEN 중복도 본다"고 적어놨지만 **실제 테스트엔 그 검사가 없었다**.
  문서가 없는 커버리지를 주장하던 것 — 이번에 실제로 붙였다.

### 2026-07-20 — 전멸을 본가식으로 (`c7dba4b`)
- 전멸하면 항상 시작 마을이던 것을 **마지막 회복 지점**으로. 본가처럼 그 장소 '안'에서 눈뜬다.
  센터 7곳을 깔아놓고 정작 전멸이 무시하던 구멍을 메웠다. 페널티도 25%→50%(본가 1세대).
- 플레이키 테스트 2건 정리 — 확률적 지표에 좁은 하드 임계값을 걸어 간헐 실패하던 것.
  게임 회귀가 아님을 재실행으로 확인한 뒤 평균화/강등으로 처리.

### 2026-07-20 — 상태 점검 + 인수인계 체계 도입
- 세션이 끊겨 "하던 게 어디 갔나" 확인 → **유실 없음**. 세 커밋 모두 src/dist 동시 반영돼 있었고
  재빌드 diff 0, `verify.sh` 전체 통과.
- 대화 컨텍스트가 날아가도 작업이 안 끊기게 **WORKLOG.md + 메모리 한 줄** 체계 도입 (이 문서).

### 2026-07-20 — 밸런스 실측 3연타
1. `7590daf` 목표 트래커 탭 자동이동이 체육관·센터에 안 먹던 버그
   (목표 타일이 비보행이라 `bfsPath`가 항상 null → 인접 보행칸으로 경로) + `longrun_test.js`
2. `cf7a439` 후반 게이트 수적 붕괴 조정 — 체육관3(4마리)·체육관4(5마리)·숲의 군주(6마리 최종진화)
3. `edafb5a` 정령 리그 엘리트4 3→4마리 + 에이스 배치 (`league_test.js` — 연속 관문은 따로 재야 한다)

### 그 이전
큰 흐름은 CLAUDE.md의 「완료된 개선」·「버그 수정 이력」에 주제별로 정리돼 있다.
시간순이 필요하면 `git log --oneline`.
