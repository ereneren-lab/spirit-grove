# 작업 로그 (WORKLOG)

> **세션 시작 시 CLAUDE.md 다음으로 이걸 읽어라.**
> CLAUDE.md = 영구 지식(아키텍처·규칙·함정, 잘 안 변함).
> WORKLOG.md = 휘발성 상태(지금 어디까지 했나, 다음에 뭘 할까).
>
> **갱신 규칙**: 커밋할 때마다 「현재 상태」를 고치고 「세션 기록」 맨 위에 한 블록 추가.
> 작업이 중간에 끊길 것 같으면 **미완 상태 그대로** 「진행 중인 작업」에 적어둘 것.

---

## 현재 상태 (2026-07-20 기준)

- **브랜치**: `main` / 작업 트리 깨끗 / stash 없음
- **최신 커밋**: `7415bde` fix: 조사로 찾은 버그 5건 (반동·HIDDEN 스코프·frz·lavaSeen·타입 면역)
- **빌드**: dist 최신. `verify.sh` 51개 전 구간 통과(구간 분할 실행).
- **진행 중인 작업**: 없음.

### 알려진 것 (다음 세션에서 헷갈리지 말 것)
- `playthrough_test.js`가 `G.busy가 풀려 있다 (true)`로 실패하면 **게임 버그가 아니라 부하 문제**다.
  15초 고정 예산으로 전투 후 애니를 기다리는데 load 30~50에선 안 끝난다. HEAD로 되돌려도 실패한다(확인함).
  고치려면 고정 예산 → 실제 시계 예산으로. 이번 범위 밖이라 손대지 않았다.
- 브라우저 테스트를 연달아 돌리면 `Target page ... has been closed`가 난다 → 구간을 나누고(`PW_FROM`/`PW_TO`)
  사이에 `pkill -9 -f headless_shell`. ⚠️ `chrom`으로 넓게 잡지 말 것(사용자 Chrome이 죽는다).
  - 부하가 크면 배치 한계가 **~10개**까지 떨어진다. 확실히 하려면 **하나씩 격리 실행 + 1회 재시도**로
    진짜 실패와 환경 플레이크를 구분할 것(이번에 52종 전부 이 방식으로 통과 확인, 환경 플레이크 3건).

### 다음에 할 만한 것

**조사로 찾았지만 아직 안 고친 것** (2026-07-20 조사, 전부 코드로 검증됨. 1~5는 `7415bde`로 처리 완료):
- **맹독이 그냥 독** (`MOVES.toxic`, 940) — 본가는 턴마다 누적 증가(1/16→2/16→…), 지금은 고정 1/8.
- **다단히트 균등 확률** (`3589` `ri(min,max)`) — 본가는 2·3회 37.5%, 4·5회 12.5%. 평균 타수가 높게 나온다.
- **전투 중 저장 → 리로드로 전투 무효화**(로드스컴). `serialize`가 `inBattle`/`foe`/`trainer`를 안 담아,
  지는 전투를 저장 후 리로드로 없던 일로 만들 수 있다. 본가는 전투 중 저장 자체가 막혀 있다.
- **상점이 진행도와 무관하게 전 품목 판매** (`SHOP` 4615~) — 진화의 돌·TM까지 첫 상점에서 다 살 수 있어
  공들여 잡은 난이도 커브를 우회한다. 본가는 뱃지 수에 따라 재고가 열린다.
- **밸런스 하네스에 상태이상이 없다** — `balance_test`/`league_test`가 `eff.status`를 아예 처리 안 한다.
  상태이상 관련 밸런스를 진짜로 재려면 하네스에 `applyStatus` 경로를 넣어야 한다.

**그 외**
- `bash scripts/playtest.sh dist/spirit_grove_3d.html 40` — 몽키 퍼즈/장기 플레이스루 (지금까지 실제 버그 3건을 이 경로로 잡았다)
- 게임필 폴리시 (로드맵 2단계 잔여)

---

## 세션 기록 (최신이 위)

### 2026-07-20 — 버그 사냥 5건 (`7415bde`)
- 서브에이전트 3명으로 전투/필드/코드위생을 훑고, 나온 주장을 전부 코드로 직접 재검증한 뒤 5건 수정.
- 제일 값어치 있던 것: **반동 공식**(입힌 피해가 아니라 자기 최대 HP 기준이라 반동기가 쓰면 손해였다)과
  **타입 상태이상 면역 부재**(불이 화상, 독이 중독).
- 부수 수확: CLAUDE.md가 "회귀가 HIDDEN 중복도 본다"고 적어놨지만 **실제 테스트엔 그 검사가 없었다**.
  문서가 없는 커버리지를 주장하던 것 — 이번에 실제로 붙였다.

### 2026-07-20 — 전멸을 본가식으로 (`c7dba4b`)
- 전멸하면 항상 시작 마을이던 것을 **마지막 회복 지점**으로. 본가처럼 그 장소 '안'에서 눈뜬다.
  센터 7곳을 깔아놓고 정작 전멸이 무시하던 구멍을 메웠다. 페널티도 25%→50%(본가 1세대).
- 플레이키 테스트 2건 정리 — 확률적 지표에 좁은 하드 임계값을 걸어 간헐 실패하던 것.
  게임 회귀가 아님을 재실행으로 확인한 뒤 평균화/강등으로 처리.

### 2026-07-20 — 상태 점검 + 인수인계 체계 도입
- 세션이 끊겨 "하던 게 어디 갔나" 확인 → **유실 없음**. 세 커밋 모두 src/dist 동시 반영돼 있었고
  재빌드 diff 0, `verify.sh` 전체 통과.
- 대화 컨텍스트가 날아가도 작업이 안 끊기게 **WORKLOG.md + 메모리 한 줄** 체계 도입 (이 문서).

### 2026-07-20 — 밸런스 실측 3연타
1. `7590daf` 목표 트래커 탭 자동이동이 체육관·센터에 안 먹던 버그
   (목표 타일이 비보행이라 `bfsPath`가 항상 null → 인접 보행칸으로 경로) + `longrun_test.js`
2. `cf7a439` 후반 게이트 수적 붕괴 조정 — 체육관3(4마리)·체육관4(5마리)·숲의 군주(6마리 최종진화)
3. `edafb5a` 정령 리그 엘리트4 3→4마리 + 에이스 배치 (`league_test.js` — 연속 관문은 따로 재야 한다)

### 그 이전
큰 흐름은 CLAUDE.md의 「완료된 개선」·「버그 수정 이력」에 주제별로 정리돼 있다.
시간순이 필요하면 `git log --oneline`.
