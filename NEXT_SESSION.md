# 다음 세션 인수인계 (2026-08-05 종료 시점)

> 읽는 순서: **이 파일 → [`WORKLOG.md`](WORKLOG.md) → [`docs/SPIRIT_GROVE_GUIDE.md`](docs/SPIRIT_GROVE_GUIDE.md)**
> 이 파일은 "지금 뭘 하면 되나"만 담는다. 왜 그렇게 됐는지는 WORKLOG, 영구 지식은 가이드.

---

## 0. 저장소 위치 (매 세션 확인할 것)

실제 위치는 **`~/Desktop/_보관/게임프로젝트/spirit-grove`** 다.
옛 경로 `~/Desktop/spirit-grove/`는 빈 껍데기이고 `.git`이 없다.
⚠️ 거기서 git을 쓰면 상위 **`~/Desktop/.git`(데스크톱 자체가 저장소다)** 으로 올라가 엉뚱한 걸 건드린다.
⚠️ 에이전트 메모리는 경로별로 저장돼 이 이동으로 끊긴다 — 그래서 인수인계를 저장소 안에 둔다.

---

## 1. 지금 상태 (전부 초록, 배포됨)

| 항목 | 상태 |
|---|---|
| 로컬 `main` | `7f3d628` · 워킹 트리 clean |
| 원격 `main` | `aa81726` (배포 스냅샷 — orphan 1개, 이력 없음) |
| 원격 `history` | `7f3d628` (커밋 이력 보존용) |
| 라이브 | https://ereneren-lab.github.io/spirit-grove/ — **오늘 수정 반영 확인됨**(Actions success + 라이브 HTML에서 직접 grep) |
| `verify.sh` | 4구간 전부 통과 (`1-22` · `23-45` · `46-58` · `59+`) |

⚠️ **배포는 `scripts/deploy.sh`가 원격 main을 force-push로 덮는다.** 이력을 남기려면
`git push origin main:history`를 **같이** 할 것(오늘도 그렇게 했다).

---

## 2. 남은 일 — 딱 하나

### fullrun 완주 검증 (미완)

```bash
cd ~/Desktop/_보관/게임프로젝트/spirit-grove
pkill -f headless_shell            # ⚠️ 먼저. 고아 프로세스가 로드를 8까지 올린 적이 있다
uptime                             # 코어당 1.0 넘으면 미루는 게 낫다 (8코어)
node scripts/fullrun_test.js dist/spirit_grove_3d.html 5400
```

**판정 기준**: 요약의 `숲의 군주 격파` 한 줄. `미격파`면 아직 못 끝낸 것이다.

**어디까지 확인됐나** — 오늘 4판을 돌렸고, 막힌 지점이 **게임에서 봇으로 옮겨갔다**:

```
3차 판:  32분 (8,18) 체육관4 앞 4뱃지
        37분 (9,23) 호숫가        ← 오늘 고친 ①②가 여기서 작동했다
        44분 (8,9)  군주의 제단 정면 ← 게임 쪽 경로는 여기까지 실제로 걸어왔다
        44~90분     문 앞에서 46분간 그라인딩만 (봇이 못 들어감)
```

- **게임은 무혐의다.** 같은 좌표·같은 상태(파도타기 중 포함)로 ArrowUp을 주면 제단에 들어간다
  — 단독 프로브 + `lord_route_test [3]`으로 확인했다. 이걸 다시 조사하지 말 것.
- 봇 결함 2건은 **오늘 코드에 넣었지만 판으로 확인 못 했다**(4차 판이 환경 문제로 43.8분에 브라우저 사망):
  ① 부딪힘 카운터 45초마다 재시도(예전엔 13회 실패 후 영구 포기)
  ② 정체 감지 — **"목표 2칸 이내인데 12분간 못 들어감"** 을 잰다
- 그래서 이번 판은 **막히면 12분 안에 빨간색으로 끊긴다.** 예산을 다 태우고 초록으로 끝나는 일은 없다.

**실패 시 다음 수순**: `TRACE=1`을 붙여 `field:bump` 줄을 본다(문 앞에서 무엇을 누르고 있는지가 찍힌다).
```bash
TRACE=1 node scripts/fullrun_test.js dist/spirit_grove_3d.html 5400 2>&1 | tee /tmp/fullrun.log
```

---

## 3. 오늘 고친 것 (다시 조사하지 말 것)

### 게임 버그 ① `findTile`의 영구 null 캐시 → `a0364fb`
`currentGoal()`은 뱃지<4면 체육관 단계에서 리턴 → `%`·`X`는 그전까지 한 번도 조회되지 않는다 →
**최초 스캔이 4번째 뱃지를 따는 순간 = 항상 체육관 안**에서 일어나고, 실내에선 `MAP_STR`이
인테리어 지도라 못 찾아 `null`이 세션 내내 캐시됐다. → **4뱃지 → 곧장 리그**(전설 준비 단계와
숲의 군주가 통째로 빠짐 = 플롯 결말 EPILOGUE에 도달 불가). 봇만이 아니라 **모든 플레이어**에게 그랬다.

### 게임 버그 ② 물로 둘러싸인 목표는 자동 이동 불가 → `a0364fb`
호수 제단(`%`)은 사방이 물인데 트래커의 대체 경로 탐색이 상하좌우 보행칸만 봤다 → 후보 0개 →
`"지금은 그곳까지 가는 길이 막혀 있다"`를 띄우면서 **동시에 그곳을 가리켰다.**
→ `bfsPath(sx,sy,tx,ty,surf)` 추가. **뭍길을 먼저 다 시도하고 없을 때만 물길을 연다**
(처음부터 열면 BFS가 최단거리를 골라 체육관 가는 길이 호수를 가로지르는 지름길로 조용히 바뀐다 —
회귀 `[8]`이 이 순서를 단정으로 강제한다).

### 회귀 `lord_route_test` `[7]` `[8]` 신설
둘 다 **수정 전 빌드에서 실패하는 것까지** 확인했다(공허한 통과 방지).
```bash
node scripts/lord_route_test.js dist/spirit_grove_3d.html    # 몇 분이면 돈다, 부하 높아도 OK
```

---

## 4. 이 세션에서 새로 배운 함정

- ⚠️ **픽스처가 상태를 어디서 조립하느냐가 커버리지를 바꾼다.**
  `lord_route_test [1]~[6]`은 상태를 **야외에서** 조립하고 트래커를 처음 읽어 캐시가 올바르게 채워진다 →
  ①을 **구조적으로 못 잡는다.** 어제 이게 초록이라 "게임 쪽 경로는 확정"이라 적었는데 틀렸다.
  **순서 의존 캐시는 순서를 재현해야 잡힌다.**
- ⚠️ **`verify.sh`는 `set -e`라 첫 실패에서 멈춘다** → 그 뒤 테스트는 아예 안 돈다.
  오늘 `door_front_test`의 오탐이 `twoturn_test` 뒤에 가려 여태 한 번도 안 돌고 있었다.
  한 건 고쳤으면 **그 구간을 다시 끝까지** 돌릴 것.
- ⚠️ **`walkable()`은 로밍 NPC 점유까지 본다** — 지도 구조를 재려는 단정에 그대로 쓰면
  "지금 NPC가 서 있다"를 "지도가 막혔다"로 읽는다(`door_front_test`가 그랬다).
- ⚠️ **부하 오탐 구분법**: 걷기가 목적지 **한 칸 앞**에서 멈추면 대개 환경이다(코어당 load 확인).
  단독으로 다시 돌려보면 통과한다. 이 맥은 Chrome·Slack만 떠 있어도 load 4~5다.
- ⚠️ **명중률 있는 기술을 단정에 쓰면 테스트가 확률로 빨개진다**(날기 95 → verify 전체가 5%).
  상태 전이를 보려는 블록은 `Math.random`을 고정할 것.

---

## 5. 그 외 대기 중인 것 (우선순위 없음 — 유저 선택)

WORKLOG의 「다음에 할 만한 것」이 그대로 유효하다. 요약하면:
지선 루트 더 파기(`route_probe.js`로 먼 쌍 먼저) · 새 지역(타일 문자 전략 먼저) ·
브라우저 테스트 추가 감축 · 게임필 폴리시.
새 종/진화 라인은 **아트가 선행**이라 지금 손대지 말 것.

---

## 6. 디자인 멀티에이전트 작업 방침 (게임과 별개)

유저는 `@design-researcher` 식으로 직접 부르지 않는다. **목표만 받으면 Orchestrator가 판별하고
`design-researcher → design-strategist → creative-producer` 순으로 자동 위임**한다.
브리프에는 CLAUDE.md 「에이전트 전달 원칙」 11개 항목을 통째로 실어 보낸다
(**서브에이전트는 이전 대화를 모른다고 가정**).
⚠️ 핵심 전략은 유저 승인 없이 확정하지 않되, **사소한 건 가정을 표시하고 진행**한다(작업을 멈추지 않는다).
