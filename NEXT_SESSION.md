# 다음 세션 인수인계 (2026-08-11 종료 시점 · 재부팅 직전 저장)

> 읽는 순서: **이 파일 → [`WORKLOG.md`](WORKLOG.md) → [`docs/SPIRIT_GROVE_GUIDE.md`](docs/SPIRIT_GROVE_GUIDE.md)**
> 이 파일은 "지금 뭘 하면 되나"만 담는다. 왜 그렇게 됐는지는 WORKLOG, 영구 지식은 가이드.

---

# ▶ 지금 바로 이어서 할 것

**사용자가 "이어서 해"라고 하면 아래를 순서대로 실행한다. 되묻지 말 것 — 확인이 필요한 지점은 ②에 명시돼 있다.**

### ① 상태 확인 (30초)
```bash
cd ~/Desktop/_보관/게임프로젝트/spirit-grove
git log --oneline -1        # 기대: b2bc0e7 (또는 그 이후) · 워킹 트리 clean
git status --short          # 비어 있어야 정상
pkill -f headless_shell     # 고아 프로세스 정리(습관)
uptime                      # 코어당 1.0 넘으면 무거운 테스트는 미룬다
```
직전 작업은 **NPC 원형 시트 경로**까지다. **검증 전 구간 통과 · 배포 완료 · 미배포분 없다.**

> 🔴 **2026-08-11 재부팅 직전 상태 — 코드 쪽은 전부 끝났고 막힌 건 "그림"뿐이다.**
> 유저가 그림을 넣기 전까지 코드로 더 할 일이 없다. 아래 §② (A)의 5줄이 유저가 할 전부다.

### ② 🔴 바로 이어서 할 것

**(A) NPC 원형 시트 12장 — ✅ 2026-08-11에 들어왔다. 끝났다.**
12/12 변환·등록·빌드·회귀·화면 확인까지 완료. 다시 손댈 것 없다.
받은 형식이 "원형당 1장"이 아니라 **대조표 한 장**이었고, 쪼개는 전처리를 새로 만들었다:
```bash
python3 scripts/split_contact_sheet.py <대조표.png> --out art_inbox/npc/_split \
        --names kid,bugkid,youth_f,youth_m,hunter,angler,miner,scholar,mystic,guard,merchant,elder
python3 scripts/make_npc_sheet.py --src art_inbox/npc/_split --out assets/art/npc_sheet
```
📌 **다음에 아트를 받을 땐 대조표로 받는 게 낫다** — 한 번에 뽑아야 화풍이 안 갈린다.
   쪼개는 건 위 스크립트가 한다(이웃 침범·라벨·부속까지 처리한다).

**(A-2) 변형 원형 5장 — ✅ 2026-08-11에 들어왔다. 끝났다.**
`kid_f`·`scholar_f`·`elder_m`·`woodcutter`·`falconer` 투입 완료. **원형 12 → 17종**,
같은 원형 겹침 **17쌍 → 11쌍**(라이벌 카이 2쌍 제외하면 15 → 9). `miner`·`hunter`는 0쌍.
틴트를 코드로 넣는 안은 **기각**했다 — 받은 그림이 이미 원색이라 `hue-rotate`가 피부색까지 돌린다.

⚠️ **NPC를 원형에 배정할 때 이름으로 성별을 짐작하지 말 것.**
   이 저장소가 쓰는 신호는 **`spr.hairlen==="long"`** 이다(도윤·나나리리·예린·복순·육아방).
   발주 문서가 `새싹 채집가 이든`을 여자아이로 옮기라고 적어뒀는데, 그 NPC의 spr은
   긴머리 없이 `hat:"cap"`이라 **문서가 틀렸다.** 코드 데이터가 이긴다.
⛔ **`youth_m`에는 변형을 주지 말 것** — 인원 1위(6명)지만 **4명이 같은 사람**(라이벌 카이가 4곳에 배치)이다.
   변형을 주면 같은 인물이 장소마다 다르게 생기는 더 나쁜 버그가 된다. 인원 표만 보면 여기서 틀린다.
⚠️ **아트가 없는 원형 id를 `NPC_ARCH`에 먼저 적지 말 것** — 그 NPC만 절차적 스프라이트로 폴백해서
   한 화면에 픽셀 시트와 절차적이 섞인다.

📌 **아직 남은 겹침 (다음에 더 받을 거리 · 우선순위 순)**
   `kid` 2쌍 · `guard` 2쌍 · `mystic` 1쌍 · `youth_f` 1쌍 · `scholar` 1쌍 · `elder` 1쌍 · `angler` 1쌍.
   숫자는 `outputs/production/2026-08-11_spirit-grove_npc-sheet-prompts.md` §7-5 표에 있다.

**(B) 신규 정령 4종 그림 4장**
데이터·진화 연결·프롬프트 전부 확정. **페인터리로 받을 것**(픽셀로 직접 뽑지 말 것).
같이 해야 하는 6가지가 문서 §3에 있다 → `outputs/production/2026-08-10_spirit-grove_new-species-design.md`

### ③ 그다음 — 유저가 고른다
1. **지역별 서사 차별화** — 도입·집 인물·안내인은 갈렸는데 중반 이후는 공용이다.
   ⚠️ 설계 문서(`…four-origins-design.md` §5)가 **"체육관 이후 분기는 안 한다"**(유지비 4배)로 정해둔 지점이다.
   **유저 확인이 필요하다** — 전면 분기냐, 지역별로 한 장면씩만 얹느냐.
2. **정령 진짜 픽셀화** — 자동 변환은 두 번 실패했다(모자이크). 손도트가 필요하고 **초반 12종부터**가 현실적.
3. **네 캐릭터로 직접 플레이** — 도입 톤이 맞는지는 읽어봐야 안다. 유저에게 권할 것.

### ④ 배포 절차 (오늘 다섯 번 이 절차를 썼다)
```bash
python3 scripts/build.py && git status --short   # dist 변화 없어야 정상(= 커밋된 코드 그대로)
bash scripts/deploy.sh
git push origin main:history                     # ⚠️ deploy는 원격 main을 force-push로 덮는다 — 이력은 따로 남긴다
gh run watch $(gh run list --limit 1 --json databaseId -q '.[0].databaseId') --exit-status
curl -s https://ereneren-lab.github.io/spirit-grove/ | grep -c 'HOME_AT'   # 라이브 확증(0이면 안 올라간 것)
```
⚠️ 실행 전 사용자에게 **한 줄로 알리고** 시작할 것(외부로 나가는 작업이다). 되묻는 게 아니라 통보면 된다.

---

---

## 0. 저장소 위치 (매 세션 확인할 것)

실제 위치는 **`~/Desktop/_보관/게임프로젝트/spirit-grove`** 다.
옛 경로 `~/Desktop/spirit-grove/`는 빈 껍데기이고 `.git`이 없다.
⚠️ 거기서 git을 쓰면 상위 **`~/Desktop/.git`(데스크톱 자체가 저장소다)** 으로 올라가 엉뚱한 걸 건드린다.
⚠️ 에이전트 메모리는 경로별로 저장돼 이 이동으로 끊긴다 — 그래서 인수인계를 저장소 안에 둔다.

---

## 1. 지금 상태 (전부 초록 · **미배포분 없음**)

| 항목 | 상태 |
|---|---|
| 로컬 `main` | `b2bc0e7` · 워킹 트리 clean |
| 원격 `history` | 로컬과 동일 — **커밋 이력 백업이 여기다** |
| 원격 `main` | 배포 스냅샷(orphan 1개, 이력 없음) |
| 라이브 | https://ereneren-lab.github.io/spirit-grove/ — `GYM_AT`·`HOME_AT`·`ORIGIN_STORY`·`_doorFx`·`_goalFx` 반영 |
| `verify.sh` | 전 구간 통과 (`pace_test`·`npc_sheet_test` 신규 등록 · 총 98개) |
| 빌드 크기 | **약 3.8MB** — ⚠️ 정령 픽셀화를 되돌려 페인터리 원본으로 복원했다(1.2MB였던 건 그 픽셀 버전) |

⚠️ **배포는 `scripts/deploy.sh`가 원격 main을 force-push로 덮는다.** 이력을 남기려면
`git push origin main:history`를 **같이** 할 것.
⚠️ **다른 기기에서 이어받을 땐 `history` 브랜치를 받는다** — `main`은 이력 없는 배포 스냅샷이다.
```bash
git clone -b history git@github.com:ereneren-lab/spirit-grove.git
cd spirit-grove && npm i && python3 scripts/build.py
```

---

## 남은 일

**출신지 인물·팻말 대사**(§▶②)가 마무리가 덜 됐다. 그다음은 유저 선택(§▶③).

---

## 2. fullrun 완주 검증 — ⛔ **접었다** (2026-08-10, 유저 결정)

### 왜 접었나
**이 맥에서 완주 검증은 비용 대비 신뢰도가 낮다.** 08-10에 두 판을 돌렸고 **둘 다 브라우저가
환경 압박으로 죽었다**(55.3분 뱃지4/4 · 36.0분 뱃지3/4). 08-05에도 43.8분에 죽었다.
📌 **08-06의 완주(36분)가 오히려 운 좋은 판이었다.** 한 판에 60분 이상을 태우고도 결론이 안 난다.

⚠️ **편차가 크다는 걸 다시 확인했다.** 2차는 부하가 더 낮았는데(코어당 0.22 vs 0.8) **오히려 일찍 죽었고**,
   체육관2는 기준(882초)보다 **빨랐다**(611초). "느려졌다 = 회귀"로 읽으면 안 된다.

### 대신 무엇으로 확인하는가
맵 수정이 실제로 위협하는 건 완주 **시간**이 아니라 **사슬이 끊기는가**다
(08-05에 `findTile` 캐시로 제단 두 곳이 사슬에서 통째로 빠진 그 사고). 그건 결정적으로 잰다.

| 테스트 | 무엇을 보는가 |
|---|---|
| `goal_reach_test` | 체육관1~4 · 호수 제단 · 군주의 제단 · 리그 · 도감까지 **목표 전부** 도달 가능한가 |
| `lord_route_test` | 숲의 군주까지의 경로(물길 포함) |
| `route_test` · `gate_test` · `door_front_test` · `origin_test` | 지선 루트 · 지역 관문 · 출입구 · 출신지 |

**맵을 고쳤으면 이 여섯 개를 돌린다.** fullrun 대신이고, 몇 분이면 끝나며 결과가 결정적이다.

### 그래도 돌리고 싶다면
```bash
pkill -f headless_shell && uptime      # 코어당 0.5 아래에서만
node scripts/fullrun_test.js dist/spirit_grove_3d.html 5400
```
⚠️ **한 판으로 판단하지 말 것.** 그리고 **다른 작업과 병행하지 말 것** — 08-10에 정령 분석을 같이 돌렸다가
   측정을 오염시켰다(node 스크립트도 CPU를 먹는다).

---

## 3. 포켓몬 갭 개선 트랙 — 묶음 A·B·C ✅ 전부 완료

유저 요청: "포켓몬과 비교해서 어떤 게 부족한지 개선점 찾아서 브리핑해".
`design-researcher → design-strategist` 순차로 돌려 브리핑했고, **유저 승인 3건을 받아 묶음 A까지 실행했다**(2026-08-06).
승인 결과: ① 타입 확대는 이번 사이클 **배제** ② 묶음 A는 **밸런스 재측정 필요**로 분류 ③ 묶음 C는 **보류**.
추가 승인: 중복 성격 2개(고집있는·명랑한)를 **본가 정의로 정정**.

- 리서치: `outputs/research/2026-08-05_spirit-grove_pokemon-gap-research.md`
- 전략: `outputs/strategy/2026-08-05_spirit-grove_pokemon-gap-strategy.md`

### 3-1. 실측 기준선 (Orchestrator가 헤드리스로 `window.SG`에서 직접 셈 — 이게 정답)

| 항목 | 값 | 항목 | 값 |
|---|---|---|---|
| 종 | 86 (전설 5 · 스타터 3) | 기술 | 108 |
| 진화 라인 | **33개** · 라인 소속 74/86 (86%) | 타입 | **10** (본가 18) |
| 성격 | **10 — 보정이 전부 atk·def·spd** | 지닌물건 | 19 |
| 트레이너 | 59 | 특성 21 · 퀘스트 9 | |

부재 타입 8: bug, ghost, dragon, dark, steel, fairy, fighting, psychic.

### 3-2. 결론 (한 줄)
**이 게임에 부족한 건 시스템이 아니라 "닿는 것"이고, 가장 값어치 있는 개선은 새 기능이 아니라 배선 누락 하나다.**

**1순위 — 성격이 물리/특수 분리를 절반만 따라갔다.**
타입 10개가 `SPEC_TYPES`로 **물리 5(normal·flying·rock·poison·ground) : 특수 5(fire·water·grass·elec·ice)** 로
정확히 이등분돼 있고 크리처에 `spa`/`spDef`가 있는데, `NATURES`의 보정은 atk·def·spd에만 걸려 있다.
→ **주 타입이 특수인 62/86종(72%)에게 성격이 사실상 무의미하다.**
⚠️ `recalc`(`src/rules/dex.js:115`)는 `m[nat.up]*=1.1` 형태라 **스탯 이름에 무관하게 동작한다** —
   `src/rules/tables.js`의 `NATURES` 테이블에 항목만 넣으면 코드 변경 없이 동작한다. (검증 완료)

### 3-3. 실행 묶음 3개
| | 내용 | 규모 |
|---|---|---|
| **A** | 성격-타입 나침반 완성 — spa/spDef 성격 5종 + 대응 `mint_*` 5종 + 회귀 재측정 | 1~2일 |
| **B** | 도달성 감사 확장 — `dead_content_test`에 "성격↔민트 매칭" · "트래커가 가리키는 곳에 실제로 갈 수 있는가" 일반화 | 반나절 + 설계 |
| **C** | 지닌물건 소폭 확장 (기존 effect 조합 재사용, 기존 값 상한 이내) | 선택 · 스킵 가능 |

⚠️ **B는 오늘 사건의 직접적 후속이다.** 오늘 버그는 `lord_route_test`라는 핀포인트 회귀로 막았지만
그건 "숲의 군주 한 지점"만 본다. 같은 사고가 다른 목표에서 또 날 수 있다.

### 3-4. ⚠️ 묶음 A는 **밸런스 불변이 아니다** (리서치의 "위험 없음"은 틀렸다)
`makeMon`(`src/rules/dex.js:107`)이 `nature:NATURES[무작위]`를 뽑고, **트레이너 팀도 같은 함수를 쓴다**
(`src/index.html:5063`). 지금까지 특수 어태커 트레이너는 성격 이득을 볼 일이 없었는데(atk/def/spd만 보정)
새 성격을 넣으면 생긴다 → **`balance_test`·`league_test` 재측정 필요.**
📌 이 프로젝트의 통념인 **"플레이어 전용 추가 = 밸런스 불변"은 `makeMon`을 공유하는 데이터에는 적용되지 않는다.**

### 3-5. 하지 말 것
- **타입 8개 확대** — 아트 병목, 6개월+ 별개 프로젝트. 아트 파이프라인이 열리기 전엔 재론하지 않는다.
- **성격 25종 풀 그리드** — 5개면 나침반이 완성된다. 나머지는 한계효용 급감.
- **진화 조건 다양화** — 라인이 이미 86% 커버라 급하지 않다.
- **트레이너 팀 깊이** — 갭인지 자체가 미확인. 조사 없이 손대지 않는다.
- WORKLOG 「일부러 안 한 것」 6건 — 뒤집을 새 근거가 안 나왔다.

### 3-6. ✅ 묶음 A 완료 (2026-08-06)
- 성격 **10 → 16**, 고유 효과 **7 → 14**. `src/rules/tables.js`만 고쳤다(`recalc`가 스탯 이름 무관하게 도므로 코드 변경 0).
- 중복이던 `고집있는`·`명랑한`을 본가 정의(`atk↑spa↓`·`spd↑spa↓`)로 복원.
- **닿게**: `조심 민트`(spa↑)·`차분 민트`(spDef↑) 추가 — 필드·상점·`ITEM_KO` 3곳. `NATURES`를 `window.SG`에 노출.
- ⚠️ `고집 민트` 설명이 `(공↑방↓)`으로 실제와 어긋나 있었다(병렬 테이블 사고) → 정정 + 회귀로 고정.
- 회귀 2종 신설(**둘 다 수정 전 빌드에서 실패 확인**): `rules_unit_test`「성격 테이블 위생」·`dead_content_test` 2단정.
- 밸런스 재측정: 게이트 승률 이동은 노이즈 범위 · 리그 완주율 13.3% → 14% · 리그 단정 7건 통과.

### 3-7. ✅ 묶음 B 완료 (2026-08-06) — `scripts/goal_reach_test.js` 신설
사슬이 만드는 목표 전부(체육관1~4 · 호수 제단 · 군주의 제단 · 리그 · 도감 완성)를 훑는다.
**게임의 실제 트래커 클릭 핸들러를 그대로 부른다**(병렬 로직 금지). `verify.sh`에 등록됐다.
- [1] 기대 목표를 가리키는가 · 진입 수단(보행/정면칸/물길)이 있는가 · 경로가 실제로 나오는가
- [3] 진행 상태 20가지 전수 · [4] **목표 조회가 실내에서 처음 일어나도 사슬이 살아남는가**
- ⚠️ 08-05의 두 사고를 실제로 잡는 것까지 확인했다(되돌린 빌드로 시험).
- ⚠️ [4]는 **반드시 새 페이지**에서 돈다 — `_tileCache`가 페이지 수명이라 같은 페이지면 무조건 통과한다.

### 3-8. ✅ 묶음 C 완료 (2026-08-06) — 지닌물건 19 → 24종
- 신규 5종(재빠른깃털·가벼운날개·무게추·무모의보석·집념의고리). **기존 effect 필드 조합만** 써서 전투 코드 변경 0.
  전부 상점 판매 = 플레이어 전용 = **밸런스 불변**(야생 풀 `WILD_HELD`는 안 건드렸다).
- 🐛 착수하자마자: **`lifeorb`의 `recoil:0.1`이 아무 데서도 안 읽히고 있었다**(키 하드코딩 + `maxHp/10`).
  표의 필드가 장식이라 반동 아이템을 새로 넣어도 조용히 아무 일도 안 일어난다 → 단일 출처로 일반화.
- ✅ 회귀 `held_effect_test` 신설 — **표에 선언한 필드마다 관측 가능한 차이를 단정**한다
  (dmg·spdx·boost는 순수 함수로, recoil·lock은 실제 전투 한 턴). 새 아이템은 자동으로 검사 대상이 된다.
- ⚠️ **`dex.js` 주석이 틀렸었다**: "트레이너 정령은 held를 덮어쓴다"는 사실이 아니다.
  `trainerMon`이 `makeMon`을 그대로 쓴다 → **트레이너 정령의 10.8%가 야생 풀에서 지닌물건을 든다.**
  즉 `WILD_HELD`를 건드리면 트레이너가 같이 세진다(성격의 `makeMon` 공유와 같은 부류).

### 3-9. ✅ 야생 지닌물건 풀 — **풀 분리로 닫았다** (2026-08-06, 유저 선택)
전략의 "야생 사냥 유인"이 남아 있었는데, 넓히면 트레이너가 같이 세지는 딜레마가 있었다.
유저가 **「풀을 둘로 분리」** 를 골라 딜레마 자체를 없앴다.
- `WILD_HELD` — 신규 5종 편입(tier2에 재빠른깃털 / tier3에 5종 전부). 휴대율 5%·9%·14%는 **그대로**.
- `TRAINER_HELD` — 분리 시점의 옛 값 **동결**. 트레이너 정령의 지닌물건 분포는 **완전 불변**이다.
- ⚠️ **트레이너 측 `makeMon` 호출부는 3곳**이다: `trainerMon` · `startDouble`의 `DB.foes` · `dbReplace`의 벤치 교대.
  `trainerMon`만 고치면 듀오 트레이너 2곳이 조용히 야생 풀을 쓴다.
- ✅ 회귀 `held_effect_test [5]` — 표 비교가 아니라 **실제 생성 경로**를 돌린다(트레이너 4710마리 · 듀오 240마리).
  되돌린 빌드 2종으로 **실패를 확인**했다.
- 📌 **트레이너를 세게 하려면 `TRAINER_HELD`를 고쳐야 하고, 그건 밸런스 재측정 대상이다.**
- 재측정 실측(플레이어 팀은 `makeMon` 기본값을 쓰므로 실제로 변한다): 게이트 승률 ±1.5%p 이내 ·
  「의도된 도전 레벨」 6게이트 전부 동일 · 리그 완주율 노이즈 범위. **재측정을 생략하지 않은 이유가 이것이다.**
- 전략 문서의 나머지(타입 확대 등)는 「하지 말 것」에 있다 — §3-5 참조.

⚠️ **서브에이전트 결과를 그대로 믿지 말 것.** 이번에 리서치 초안에 사실 오류 5건이 있었고
(진화 라인을 8개로 오독 — 07-29 커밋이 *추가한* 8개를 총량으로 읽었다), 전략가가 다시 리서치의
밸런스 판단을 정정했다. **수치는 게임을 띄워 `window.SG`에서 직접 세는 게 가장 빠르고 확실하다.**

---

## 4. 오늘 고친 것 (다시 조사하지 말 것)

### 게임 버그 ① `findTile`의 영구 null 캐시 → `a0364fb`
`currentGoal()`은 뱃지<4면 체육관 단계에서 리턴 → `%`·`X`는 그전까지 한 번도 조회되지 않는다 →
**최초 스캔이 4번째 뱃지를 따는 순간 = 항상 체육관 안**에서 일어나고, 실내에선 `MAP_STR`이
인테리어 지도라 못 찾아 `null`이 세션 내내 캐시됐다. → **4뱃지 → 곧장 리그**(전설 준비 단계와
숲의 군주가 통째로 빠짐 = 플롯 결말 EPILOGUE에 도달 불가). 봇만이 아니라 **모든 플레이어**에게 그랬다.

### 게임 버그 ② 물로 둘러싸인 목표는 자동 이동 불가 → `a0364fb`
호수 제단(`%`)은 사방이 물인데 트래커의 대체 경로 탐색이 상하좌우 보행칸만 봤다 → 후보 0개 →
`"지금은 그곳까지 가는 길이 막혀 있다"`를 띄우면서 **동시에 그곳을 가리켰다.**
→ `bfsPath(sx,sy,tx,ty,surf)` 추가. **뭍길을 먼저 다 시도하고 없을 때만 물길을 연다**
(처음부터 열면 BFS가 최단거리를 골라 체육관 가는 길이 호수를 가로지르는 지름길로 조용히 바뀐다 —
회귀 `[8]`이 이 순서를 단정으로 강제한다).

### 회귀 `lord_route_test` `[7]` `[8]` 신설
둘 다 **수정 전 빌드에서 실패하는 것까지** 확인했다(공허한 통과 방지).
```bash
node scripts/lord_route_test.js dist/spirit_grove_3d.html    # 몇 분이면 돈다, 부하 높아도 OK
```

---

## 5. 이 세션에서 새로 배운 함정

- ⚠️ **픽스처가 상태를 어디서 조립하느냐가 커버리지를 바꾼다.**
  `lord_route_test [1]~[6]`은 상태를 **야외에서** 조립하고 트래커를 처음 읽어 캐시가 올바르게 채워진다 →
  ①을 **구조적으로 못 잡는다.** 어제 이게 초록이라 "게임 쪽 경로는 확정"이라 적었는데 틀렸다.
  **순서 의존 캐시는 순서를 재현해야 잡힌다.**
- ⚠️ **`verify.sh`는 `set -e`라 첫 실패에서 멈춘다** → 그 뒤 테스트는 아예 안 돈다.
  오늘 `door_front_test`의 오탐이 `twoturn_test` 뒤에 가려 여태 한 번도 안 돌고 있었다.
  한 건 고쳤으면 **그 구간을 다시 끝까지** 돌릴 것.
- ⚠️ **`walkable()`은 로밍 NPC 점유까지 본다** — 지도 구조를 재려는 단정에 그대로 쓰면
  "지금 NPC가 서 있다"를 "지도가 막혔다"로 읽는다(`door_front_test`가 그랬다).
- ⚠️ **부하 오탐 구분법**: 걷기가 목적지 **한 칸 앞**에서 멈추면 대개 환경이다(코어당 load 확인).
  단독으로 다시 돌려보면 통과한다. 이 맥은 Chrome·Slack만 떠 있어도 load 4~5다.
- ⚠️ **명중률 있는 기술을 단정에 쓰면 테스트가 확률로 빨개진다**(날기 95 → verify 전체가 5%).
  상태 전이를 보려는 블록은 `Math.random`을 고정할 것.

---

## 6. 그 외 대기 중인 것 (우선순위 없음 — 유저 선택)

WORKLOG의 「다음에 할 만한 것」이 그대로 유효하다. 요약하면:
지선 루트 더 파기(`route_probe.js`로 먼 쌍 먼저) · 새 지역(타일 문자 전략 먼저) ·
브라우저 테스트 추가 감축 · 게임필 폴리시.
새 종/진화 라인은 **아트가 선행**이라 지금 손대지 말 것.

---

## 7. 디자인 멀티에이전트 작업 방침 (게임과 별개)

유저는 `@design-researcher` 식으로 직접 부르지 않는다. **목표만 받으면 Orchestrator가 판별하고
`design-researcher → design-strategist → creative-producer` 순으로 자동 위임**한다.
브리프에는 CLAUDE.md 「에이전트 전달 원칙」 11개 항목을 통째로 실어 보낸다
(**서브에이전트는 이전 대화를 모른다고 가정**).
⚠️ 핵심 전략은 유저 승인 없이 확정하지 않되, **사소한 건 가정을 표시하고 진행**한다(작업을 멈추지 않는다).

---

## 8. 포켓몬 갭 2차 — 기술 단위 물리/특수 ✅ (2026-08-06)

### 8-1. 갭이 **아닌** 것 (다시 조사하지 말 것 — 실측으로 기각)
| 항목 | 실측 |
|---|---|
| 타입 상성표 | 100칸 중 45%가 비등배 — **본가(약 38%)보다 촘촘하다** |
| 기술 습득 경로 | 108개 중 못 배우는 건 `struggle`(PP 소진 폴백)뿐 · 기술머신 44개 전부 입수 가능 |
| 특성 21종 | 전부 전투 코드에 배선됨(`insomnia`·`immunity`·`waterveil`는 `battle.js:67` 객체 키) |
| 노력치·개체값·육아방 | 전부 있다 — 알 기술·개체값·성격 유전까지 |
| 진화 조건 | 4종류(레벨 34 · 아이템 3 · 친밀도 1 · 분기 1) |
| 강한 스탯 쪽 STAB이 없는 종 | **0마리** |

### 8-2. 무엇을 고쳤나
`damage()`가 `SPEC_TYPES[move.type]`으로 계열을 정했다(3세대 이전 방식) → `MOVES.cat` **덮어쓰기** +
`moveCat()` **단일 출처**로 옮겼다. 계열을 묻는 곳은 `damage()`와 까칠한피부 **2곳뿐**이고 둘 다 거친다.
UI 3곳(전투 메뉴 · `moveSummary` · 기술 배우기/잊기)에 "물리/특수/변화"를 노출했다 — **전엔 어디에도 없었다.**

- 10타입 중 **7타입이 물리·특수를 모두 갖게 됐다**(전엔 전 타입이 한쪽뿐).
- 죽은 STAB 해소: 집게왕(아쿠아제트 145→224+선제) · 화염나방(바람일격 특수화) · 마그마룡(열창 물리 TM).
- ⚠️ **뇌전매·푸르사는 안 풀린다** — 계열이 아니라 **위력 차이**였다. 정직하게 남겼다.

### 8-3. 📌 다음에 계열을 건드릴 때 반드시 읽을 것
**"본가와 같게"가 곧 개선이 아니다.** 이 게임 종족값은 타입이 계열을 강제한다는 전제로 짜여 있다.
처음에 본가 기준으로 13개를 뒤집었더니 여럿이 **그 기술을 배우는 종을 약화**시켰다
(파괴광선 특수화 → 배우는 22종 평균 atk 24.0 > spa 20.8이라 전부 손해. 덩굴채찍·얼음뭉치·
플레어드라이브·와일드볼트·머드샷도 같은 이유로 되돌림).
→ 넣는 기준은 둘뿐이다: **① 배우는 종에게 이득이고 본가와도 맞는 것 ② TM 전용(배우는 종 0 =
트레이너·야생 미사용 = 밸런스 불변)이라 없던 선택지를 만드는 것.**
⚠️ **새 항목 전에 그 기술을 배우는 종의 평균 atk/spa를 먼저 재라.**

### 8-4. ⚠️ 밸런스 — 종반이 실제로 어려워졌다 (노이즈 아님)
`balance_test` 3000판: 체육관4·숲의 군주 **+1~4%p**(플레이어 유리) · 챔피언 **-1~4.6%p**.
`league_test` 1500판: Lv54 완주율 35.9%→31.8%(전설 없음) · 53.3%→46.8%(전설 1).
📌 **원인: 챔피언과 리그 챔피언 아리엘의 선두가 둘 다 집게왕이다.** 죽은 STAB을 고치니 트레이너가 같이 세졌다.
단정은 양쪽 전부 통과 · 「의도된 도전 레벨」 6게이트 그대로. **되돌리려면 집게왕 `moves`의 `aquajet`→`hydro`.**

### 8-5. 회귀 `movecat_test` (verify.sh 등록)
- [1] 선언한 계열이 **실제 `damage()`** 에 반영되는가(공격기 69개 전수)
- [2] `cat` 없는 기술은 타입 기본과 일치하는가(하위호환 99개)
- [3] **까칠한피부가 `moveCat`을 거치는가** — 실제 전투 한 턴으로 잰다(병렬 판정 부활 감지)
- [4] UI가 계열을 보여주는가 · [5] 한쪽 계열뿐인 타입 집합 고정(`normal`·`grass`·`rock`)
- ⚠️ **되돌린 빌드 2종으로 실패 확인**했다: `damage()`를 되돌리면 [1]이, **까칠한피부만** 되돌리면 [3]이 걸린다.

---

## 9. 주인공 픽셀 전환 — 코드 준비 ✅ (2026-08-06) · **이미지 대기 중**

유저: "주인공들 너무 이질감 들어. 2D, 픽셀로 만들 수 있게끔 프롬프트 짜줘" → 이미지 생성 중.

### 9-1. 왜 이질감인가 (화면을 띄워 확인한 것 — 다시 조사하지 말 것)
오버월드 한 화면에 **화풍이 넷** 섞여 있다.

| 요소 | 화풍 | 출처 |
|---|---|---|
| **주인공** | 애니 일러스트 + **베이지 팔각 배경판** | AI 이미지 `HERO_ART` |
| NPC·트레이너 | **픽셀** | **코드가 그린다**(`trainerSpr` → 색 스펙) |
| 정령 86종 | 수채 페인터리 | AI 이미지 `PAINT_ART` |
| 가구·UI | 플랫 아이콘 · 이모지 | 코드·이모지 |

📌 **NPC는 이미 픽셀이다.** 주인공만 튄다 → "주인공을 픽셀로"가 옳은 방향이다.
지금 아트 결함 4가지: 배경판 잔존(`hero/0`·`hero_back/0`) · **4종이 서로도 불일치**(`hero/1`엔 판 없음) ·
발이 캔버스 아래 끝에 안 닿아 **떠 보임** · 코드의 타원 그림자와 **이중 그림자**.

### 9-2. 코드는 준비됐다 — `heroDrawSpec(srcW, ts)` (`src/rules/util.js`)
원래 두 가지가 막고 있었다: `imageSmoothingEnabled`가 소스에 아예 없어 기본값 `true`(도트가 뭉개진다) ·
그리기 크기가 32의 정수배가 아님(실측 75/67/81px → 격자가 울퉁불퉁).
⚠️ **둘을 그냥 켜면 지금의 고해상도 일러스트가 오히려 거칠어진다.** 그래서 **원본 크기 하나로 갈리게** 했다.

| 원본 | 크기 | 보간 |
|---|---|---|
| **≤ `PIXEL_SRC_MAX`(64px) = 픽셀** | 32의 정수배로 **내림** → 실측 대역에서 항상 **64** | **끔**(최근접) |
| 그보다 크면 = 일러스트 | `ts*0.98` (예전 그대로) | 켬 |

→ **플래그 데이가 없다. 32×32 webp를 넣는 순간 자동으로 픽셀 경로로 넘어간다.**
⚠️ 반올림이 아니라 **내림**이다 — 반올림하면 뷰포트별로 64↔96을 오가 크기가 50% 튄다(타일 실측 68~83px).
⚠️ `charPreview`(캐릭터 선택)도 **같은 함수**를 쓴다. 따로 계산하면 선택 화면과 실제가 갈린다.
✅ `rules_unit_test`「주인공 스프라이트 그리기 규격」 8단정 + **32×32 체커보드 실주입 결과 중간색 0.00%**
   (스펙만 맞고 실제로는 안 도는 경우를 막으려고 그려서 쟀다).

### 9-3. 프롬프트·규격 문서
`outputs/production/2026-08-06_spirit-grove_hero-pixel-art-prompts.md`
— 규격표 · 공통 프리픽스/네거티브 · **프롬프트 8종**(4캐릭터 × 앞/뒤) · 검수 체크리스트.
색은 게임 데이터 `CHARS`의 실제 hex를 썼다(선택 화면 절차적 미리보기가 같은 색을 쓴다 — 바꾸면 어긋난다).
📌 코드에서 유도한 핵심 제약: 아트는 **2장뿐**이고 왼쪽은 앞모습 좌우 반전이라
**한 장이 "정면"과 "오른쪽"을 겸한다** → **정면에서 오른쪽으로 15~25° 튼 자세**가 규격이다.

### 9-4. 아트 0장 대안 (실제로 만들어 비교했다)
`HERO_ART`를 비우면 코드가 이미 가진 절차적 워커(`_walker`)로 되돌아간다.
스크린샷 확인 결과 **NPC와 화풍이 정확히 일치하고 배경판·뜸이 즉시 사라진다.** 빌드도 214KB 가벼워진다.
대신 디테일이 확연히 적다(후드 실루엣 + 단색 몸통).
📌 **픽셀 아트를 만들 이유는 "일관성"이 아니라 "일관성 + 디테일"이다** — 일관성만 원하면 공짜로 얻는다.

---

## 10. 2026-08-07 — 픽셀 전환 완료 + 지도/시작 개선 (오늘 한 것 전부)

### 10-1. 아트가 통째로 픽셀이 됐다
- **주인공 4명 걷기 시트** — 3열(정지/걸음A/걸음B) × 3행(아래/위/옆), 칸 64px.
  `scripts/make_hero_sheet.py`가 생성형 아트 → 시트로 만든다(배경 제거·그림자 제거·도트화·걸음 합성).
- **정령 86종** — `scripts/pixelize_creatures.py`로 일괄 변환. **원본은 `art_inbox/creatures_src/`**.
  ⚠️ 입출력이 같으면 스크립트가 거부한다(이미 줄어든 걸 또 줄이면 못 되돌린다).
- 📌 **아트만 바꾸면 안 된다** — 확대되는 곳마다 보간이 켜져 있어 전부 뭉갠다.
  `.sprite`·`img.cart`·팔로워 `drawImage`·주인공 `drawImage` 네 곳을 최근접으로.
- 빌드 **3.9MB → 1.2MB**.

### 10-2. 출입구가 안 보이던 것 (유저가 여러 번 제보한 건)
원인은 **규칙과 안내가 어긋난 것**이었다 — 07-28에 건물을 정면 진입만 가능하게 바꿔놓고
어느 칸이 정면인지 알려주는 시각 신호를 안 넣었다. 문도 타일의 20%×32%(약 15×25px)라 안 보였다.
- `Map2D._doorFx` — 건물 7종(`DOOR_TILES`)에 문틀·손잡이·문등 + **정면 칸에 발판과 위 화살표**
- 자연 입구 9종(`NATURE_ENTRY`)엔 **다른 신호**(빛+고리). 방향 제약이 없어 발판·화살표를 주면 거짓말이 된다
- 🐛 **`$`(지선 루트 입구)가 두 목록 어디에도 없어 표시가 통째로 빠져 있었다.** 유저가 "집에서 왼쪽으로
  갔을 때 갑자기 들어가진다"고 한 게 정확히 그 자리다 → `NATURE_ENTRY`에 편입.
  회귀 `door_front_test [7]`이 **맵에 있는 입구 타일이 전부 표시 대상인지** 자동으로 잰다.
- 건물이 타일보다 작던 것(가로 72%·세로 54%) → **접지선 고정으로 1.45배**.
  ⚠️ 위로 늘리면 안 된다 — 문 20곳 중 **16곳이 위 칸이 걸을 수 있는 풀밭**이라(실측) 덮으면 거짓말이 된다.
- `Map2D._goalFx` — 지도에 목표 표식(화면 안이면 고리+핀, 밖이면 가장자리 화살표+칸수)

### 10-3. 📌 `walkable`을 지형/점유로 쪼갰다 (구조 개선)
한 함수가 **지형 판정과 로밍 NPC 점유를 섞어** 봐서, 이 저장소가 같은 함정을 두 번 밟았다
(관문 기둥이 NPC를 따라 춤춤 · `door_front_test [5]`가 간헐 실패 — 재스캔 4회로도 못 막았다.
NPC가 45% 확률로 제자리에 머물러 3초를 버틴다).
→ **`terrainWalkable`(지도만) / `walkable`(지도+점유)**. **구조를 묻는 단정은 terrainWalkable을 쓸 것.**

### 10-4. 시작 페이싱 + 출신지 4곳
- 집 앞 풀숲을 물리고(2칸), 집 옆 루트 입구를 정령센터 옆으로 옮겼다
- **출신지 4벌** — 정령 연구소(리오)·길가 야영지(미나)·언덕 농가(토리)·숲가 빈터(엘).
  설계 근거는 `outputs/strategy/2026-08-07_spirit-grove_four-origins-design.md`
- ⚠️ **오버월드 지역을 나누는 원안은 불가능하다** — 난이도가 y 하나로 결정되는데(마을 Lv5·초원 Lv7·숲 Lv13)
  스타터가 Lv5라 숲 이상에서 출발하면 즉사한다. 지도 재설계는 별개 공사다.
- 회귀 `origin_test` — 넷 전부를 실제로 굴려 시작·도입 화자·나오는 자리·재입장을 잰다

### 10-5. ⚠️ 오늘 배운 함정
- **맵에 입구를 추가하면 표시 목록(`DOOR_TILES`/`NATURE_ENTRY`) 등록을 반드시 같이.**
  안 하면 조용히 표시가 빠진다. 이제 `door_front_test [7]`이 잡는다.
- **입구는 `#`(벽)에 놓는다** — `$`·`H` 둘 다 비보행이라 통행성이 안 변한다. 착지 칸은 원래 걸을 수 있던 곳으로.
- **좌표가 세 곳에 흩어져 있다**: `ROUTE_AT`/`HOME_AT` · `HOME_POS` · 인테리어 `exits`.
  하나만 고치면 조용히 어긋난다.
- **테스트가 문구를 박아두면 서사를 못 고친다.** `opening_test`가 "아침 햇살"·"엄마"를 단정하고 있어
  출신지를 넣자 빨개졌다 → **역할**(장소 묘사인가/배웅인가)을 단정하도록 전환.
- 아트 원본은 **절대 덮어쓰지 않는다**(`art_inbox/`로 옮기고 변환).
